{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": true,
  "properties": {
    "bundleId": {
      "description": "Opaque stable identifier assigned by the authority that owns the object.",
      "maxLength": 256,
      "minLength": 1,
      "type": "string"
    },
    "contractVersion": {
      "description": "Strict semver contract version carried by every wire object. Decoders accept the same major version; any other version is a typed error.",
      "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)(?:-[0-9A-Za-z-]+(?:\\.[0-9A-Za-z-]+)*)?(?:\\+[0-9A-Za-z-]+(?:\\.[0-9A-Za-z-]+)*)?$",
      "type": "string"
    },
    "description": {
      "description": "Human-readable purpose of this grouping.",
      "maxLength": 4096,
      "minLength": 1,
      "type": "string"
    },
    "evidenceContentIds": {
      "description": "Evidence content ids in listed order; entries should be unique.",
      "items": {
        "description": "sha-256 content address (64 lowercase hex). Normative derivation: the frozen AISE-CONTENT-V1 canonical encoding (see apps/android ContentIdentity).",
        "pattern": "^[0-9a-f]{64}$",
        "type": "string"
      },
      "type": "array"
    }
  },
  "required": [
    "contractVersion",
    "bundleId",
    "description",
    "evidenceContentIds"
  ],
  "type": "object"
}
