{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": true,
  "properties": {
    "contractVersion": {
      "description": "Strict semver contract version carried by every wire object. Decoders accept the same major version; any other version is a typed error.",
      "pattern": "^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)(?:-[0-9A-Za-z-]+(?:\\.[0-9A-Za-z-]+)*)?(?:\\+[0-9A-Za-z-]+(?:\\.[0-9A-Za-z-]+)*)?$",
      "type": "string"
    },
    "description": {
      "description": "What is missing/weak/ambiguous and why it matters.",
      "maxLength": 4096,
      "minLength": 1,
      "type": "string"
    },
    "gapId": {
      "description": "Opaque stable identifier assigned by the authority that owns the object.",
      "maxLength": 256,
      "minLength": 1,
      "type": "string"
    },
    "kind": {
      "enum": [
        "MISSING",
        "WEAK",
        "AMBIGUOUS"
      ],
      "type": "string"
    },
    "subjectRef": {
      "description": "Optional reference to the entity whose knowledge is gapped.",
      "maxLength": 256,
      "minLength": 1,
      "type": "string"
    }
  },
  "required": [
    "contractVersion",
    "gapId",
    "kind",
    "description"
  ],
  "type": "object"
}
