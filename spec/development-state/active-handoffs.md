# AISE v2 Active Handoffs

## Current architecture handoff

The repository is in **architecture baseline / pre-implementation** state.

### Initial activation

- AISE-001 — ZAI — repository/runtime foundation — ACTIVATED
- AISE-002 — GEMINI — Android foundation — ACTIVATED

All later items remain blocked until their hard dependencies are merged. Do not infer readiness from roadmap position alone.

### Tech Lead next action

Dispatch up to two workers initially (001 and 002). When both merge and the machine state is synchronized, activate 003. The following wave can then expose three independent surfaces (004, 005 and 003 only after the shared contract is finalized; otherwise keep the wave sequential).

### Architecture facts

- Reality Graph is canonical.
- Evidence Graph is provenance authority.
- Assurance is readiness authority.
- Verification is formal deterministic authority.
- Device capability changes capture strategy/operator burden, never required assurance.
- BOQ is a connected domain graph, not canonical reality.
- Intervention states are proposed until supported by execution evidence.

### Simulation conclusion

The 72-archetype / 576-project competitive simulation forecasts materially stronger adoption if AISE is the primary cross-system interface rather than a rip-and-replace platform. Large-firm exclusivity remains constrained by incumbent systems of record; small-firm exclusivity is substantially higher. See `docs/adoption-simulation.md`.
