# AISE v2 Development State

`program-state.json` is the machine-readable progress/evidence authority.

The Tech Lead must update it after every accepted merge and use it to recompute work eligibility. `active-handoffs.md` records the current continuation point for fresh agents.

A roadmap/state mismatch is invalid. Conversation history is never required.
