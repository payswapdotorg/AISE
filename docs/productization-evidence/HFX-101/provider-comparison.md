# HFX-101 — Provider Comparison: the MapAnything double vs the reference path

The **provider comparison record** the work order demands: the SAME corpus
evaluated through (a) the registered MapAnything candidate (the
deterministic in-repo double standing in for the model) and (b) the
EXISTING deterministic/reference path (the reality-eval testkit's
providers — imported, never modified), over the SAME evidence fixtures,
joined per Layer-1 capability lane on the control plane's
`benchmarkComparabilityKey`. Everything below is the committed comparison
data of `tools/mapanything-eval/fixtures/expected-outcomes.json`
(regenerated byte-identically by
`bun backend/api/src/mapanything-eval/regenerate.ts`; every record id and
manifest id re-derives from its own content — verified by the tools
runner's content-addressing checks).

## The honest note (carried by the record itself)

**The MapAnything side exercises a DETERMINISTIC IN-REPO DOUBLE standing
in for the registered candidate — the per-variant outcome counts and
metric deltas measure the scripted demonstration capability profile, NOT
measured model behavior.** The reconstruction-lane deltas are exactly 0
because the double's well-grounded reconstruction core is the shared
deterministic least-squares fit over the same ground-truth-blind capture
view as the reference double (the provider-substitution demonstration:
swapping the provider preserves the canonical semantics). The depth-lane
deltas reflect the double's documented metric-depth deviation profile
(+0.1 % scale, +2 mm offset). The explicit-gate cells (degraded-evidence,
failed-invocation, unsupported-task-combination) are declared behaviors of
the REGISTERED MapAnything profile with no counterpart in the reference
double (which reconstructs from the capture view unconditionally), so the
reference rows cover the shared content tasks only. **A future real-model
run slots into the same benchmark ids, capability lanes and comparability
keys without any schema change.**

## Lane 1 — reconstruction (`reality-eval-reconstruction/1|reconstruction`)

### Outcome counts

| | MapAnything (registered candidate) | reference path (`fixture-reconstruction-provider` v1) |
|---|---|---|
| runs | 7 (3 content + 4 explicit refusals) | 3 (3 content) |
| verdicts | pass × 7 | pass × 3 |
| failure kinds exhibited | `unsupported-data` × 3, `resource-exhaustion` × 1 | (none) |
| behavior-matrix cells passed | grounded-pass, degraded-evidence, failed-invocation, unsupported-task-combination | grounded-pass |
| consolidated record | `2fe1f08f6ae170961fa744795852aca52765c207a8bf0cb55da5ad1c823fca4d` | `457e2904ee4b9601593d5c0b684ad8028bf059c6bca11bba8cbe1a1f2330a839` |
| provenance manifest | `bb9c7e12732d5449558d345b55fcfce39c339742aa5b239942f89229febf236c` | `eaa0a694c3e68cc91b711b985f429c465eed2dcd00a1d4a30443302009aca0a3` |
| comparability key | `reality-eval-reconstruction/1\|reconstruction` | (identical — the join) |

### Geometry/registration metric deltas vs the canonical expectations
(shared content tasks; mean |value| per metric per task; delta = candidate − reference)

| Task | metric | unit | MapAnything | reference | delta |
|---|---|---|---|---|---|
| `recon-multiimage-flagship-grounded-001` | plane_fit_rms | m | 0.00196116 | 0.00196116 | **0** |
| `recon-multiimage-flagship-grounded-001` | registration_error | m | 0.00025714 | 0.00025714 | **0** |
| `recon-multiimage-flagship-grounded-001` | scale_error | ratio | 0.00003821 | 0.00003821 | **0** |
| `recon-multiimage-flagship-grounded-001` | dimension_error | m | 0.00011558 | 0.00011558 | **0** |
| `recon-multiimage-flagship-grounded-001` | object_volume_error | ratio | 0.00165093 | 0.00165093 | **0** |
| `recon-multiimage-midrange-grounded-002` | plane_fit_rms | m | 0.00795952 | 0.00795952 | **0** |
| `recon-multiimage-midrange-grounded-002` | registration_error | m | 0.00119559 | 0.00119559 | **0** |
| `recon-multiimage-midrange-grounded-002` | scale_error | ratio | 0.00018750 | 0.00018750 | **0** |
| `recon-multiimage-midrange-grounded-002` | dimension_error | m | 0.00056608 | 0.00056608 | **0** |
| `recon-multiimage-midrange-grounded-002` | object_volume_error | ratio | 0.01441749 | 0.01441749 | **0** |
| `recon-registration-twopass-grounded-003` | plane_fit_rms | m | 0.00196116 | 0.00196116 | **0** |
| `recon-registration-twopass-grounded-003` | registration_error | m | 0.00025714 | 0.00025714 | **0** |
| `recon-registration-twopass-grounded-003` | scale_error | ratio | 0.00003821 | 0.00003821 | **0** |
| `recon-registration-twopass-grounded-003` | dimension_error | m | 0.00011558 | 0.00011558 | **0** |
| `recon-registration-twopass-grounded-003` | object_volume_error | ratio | 0.00165093 | 0.00165093 | **0** |

**Every reconstruction-lane delta is exactly 0.** The double's
well-grounded reconstruction core is delegated to the EXISTING reference
double over the same ground-truth-blind capture view, so both variants
produce byte-identical canonical geometry over the same evidence — the
provider-substitution demonstration (the architecture-lock's "a provider
swap must preserve domain semantics"), with the committed thresholds
(gates-1 flagship: plane_fit_rms ≤ 4 mm, registration_error ≤ 5 mm,
scale_error ≤ 1 %, dimension_error ≤ 20 mm, object_volume_error ≤ 5 %;
midrange: 16 mm / 20 mm / 2.5 % / 40 mm / 15 %) satisfied by both.

## Lane 2 — depth (`reality-eval-depth/1|depth`)

### Outcome counts

| | MapAnything (registered candidate) | reference path (`fixture-reality-depth-provider` v1) |
|---|---|---|
| runs | 1 (1 content) | 1 (1 content) |
| verdicts | pass × 1 | pass × 1 |
| failure kinds exhibited | (none) | (none) |
| consolidated record | `001a9e5ec276bab3628b30a60f225f202a06bd9fd56c7d7383ee943bee4e4f2a` | `08b205bcb808a899e41fc20e5be31f22b58cadc86ec236e3421dcf2484f9a8f9` |
| provenance manifest | `ffaa9645133ae455ac63db6846050410dd5feb1074a6c6b88333041b9e986204` | `a41f3fb80098a8b5c55c20fd09bfe971ec35778743235c4b55dc3c11b95569a6` |
| comparability key | `reality-eval-depth/1\|depth` | (identical — the join) |

### Depth metric deltas vs the canonical expectations
(shared content task: `depth-metric-wall-grounded-004`)

| metric | unit | MapAnything | reference | delta |
|---|---|---|---|---|
| depth_mae_m | m | 0.00395625 | 0 | **0.00395625** |
| depth_max_error_m | m | 0.005 | 0 | **0.005** |
| criteria_satisfied | ratio | 1 | 1 | 0 |

The reference path reproduces the documented depth truth exactly (mae 0,
max 0). The MapAnything double's metric-depth answers carry the
**documented deviation profile** (+0.1 % scale, +2 mm offset → mae
0.00395625 m, max 0.005 m) — the measurable, non-zero delta of the lane,
comfortably within the committed depth thresholds (mae ≤ 0.01 m, max ≤
0.05 m) with `criteria_satisfied = 1` on both sides.

## The declared latency/resource + uncertainty profiles (every row)

| | MapAnything (registered candidate) | reference path |
|---|---|---|
| accelerator | gpu | none |
| declared memory | 49152 MiB (recommended envelope) | 32 MiB |
| declared latency p50 / p95 | 1500 ms / 6000 ms | 1 ms / 5 ms (recon) · 0.5 ms / 1 ms (depth) |
| cost model | `per-invocation` — 0.004 USD (declared self-hosted GPU amortization estimate) | `none` — 0 |
| uncertainty calibration | `measurement-uncertainty` (per-answer σ; confidence separate) | `none-declared` (no per-answer uncertainty — recorded, never fabricated) |
| per-task σ (candidate) | flagship 0.002 m · midrange 0.008 m · registration 0.002 m · depth 0.005 m (answer-sigma); degraded refusals cite the σ ≥ 0.05 m bound | (none declared) |

The declared resource observations ride along on every consolidated
record (`resourceObservations` — declared profile metadata, never sensed);
the uncertainty characteristics ride along on every comparison row (the
declared calibration + the per-task σ expectations and the per-answer
declared sigmas). The reference path's no-per-answer-uncertainty
characteristic is its declared honest profile — the comparison records
it; the candidate fabricates nothing either way.

## The join (comparability)

Both variants' consolidated records carry the SAME pinned Layer-1
benchmark ids and capabilities as the existing reference path's committed
suite — `reality-eval-reconstruction/1|reconstruction` and
`reality-eval-depth/1|depth` — so the control plane's
`benchmarkComparabilityKey` joins them into one table per lane. A future
real-model run (a new technology version of the same provider id,
executions submitted through the same declared I/O contracts) produces
records with the same keys that join this table **without any schema
change** — that is the exit gate's "comparable benchmark record with
provenance, uncertainty, resource profile and explicit failure behavior".
