=== POST-002 COMPLETION REPORT ===
- Work Item ID: POST-002 (issue #16)
- Base SHA (clone HEAD, live-resolved): `6a223f9fe74ea94138c4b62db8b228a615522e01` (2026-09-25 14:58:46 +0000, "deploy: exact-SHA production redeploy pinned — dpl_B7dnBrzQ2P3FzbVgdv8AhF814rU1 at 7968026…"). The dispatch-time value `4065037bdf825c3848638e39ff5e72cfc2f87662` is an ancestor of the live HEAD; per the work order, the live value wins and is recorded here.
- Head SHA: `6a223f9fe74ea94138c4b62db8b228a615522e01` — same; no repository mutations were made (clone working tree clean at all checks: `git status --porcelain` empty; no commits, no branches, no pushes, no file writes inside the clone).
- Protected surfaces touched: none — read-only wave. No AISE source code was modified (device-validation worker, not the architect; defects below are REPORTED with proposed governed fixes, never patched).

## Lane classifications (upfront, first-class results)

- **PHYSICAL = BLOCKED_NO_PHYSICAL_HARDWARE** — this sandbox has no attached Android device and no `adb` (verified: `which adb` → NOT FOUND). Absence of hardware is an honest BLOCKED state, not a hidden failure. No physical evidence is claimed.
- **EMULATED = BLOCKED_NO_TOOLCHAIN** — no Android SDK, no emulator, no AVD (`sdkmanager`/`emulator` NOT FOUND; no `$ANDROID_HOME`; no SDK directories), and **no `/dev/kvm`** — a KVM-less sandbox cannot run an emulator at all, exactly the condition the prior campaign recorded as `BLOCKED_NO_KVM` (PROD-033 m-run row 19, citing PROD-032 §3). Zero minutes were spent attempting to build an emulator toolchain, per the ≤20-minute rule and the impossibility of the target.
- **JVM-DETERMINISTIC = BLOCKED_NO_TOOLCHAIN** (time-boxed abort; see Section B) — JRE-only environment (OpenJDK 21.0.12.1 runtime present; **`javac` absent** — `/usr/lib/jvm/java-21-openjdk-amd64/bin/` contains only `java`, `keytool`, `jpackage`, `rmiregistry`), no `gradle` binary, no dependency cache; the Gradle-wrapper distribution download was throttled to ~11 KB/s and the 20-minute hard box expired before Gradle executed anything. No test counts are claimed.
- **STATION = BLOCKED_NO_CREDENTIALS** (scripts verified CURRENT — see Section D) — `E2B_API_KEY` is not in this wave's scope; no authenticated cloud operation was attempted; no fresh station run was performed or claimed.

Prior evidence consulted (honestly classified, never upgraded): PROD-033 journey runs (`docs/productization-evidence/PROD-033/runs/m-2026-09-25T14-57-50Z.md` — the M journey at SHA 7968026: 19 PASS / 0 FAIL / 1 BLOCKED_NO_KVM, all rows honestly classed, emulator lane BLOCKED, fresh-E2B-run not performed), PROD-032 station records (Gradle trio at delivery SHA `6728c3b`: `:core:test` 34 suites/387 tests/0 failures; `:app:test` 176 tests/0 failures/2 skipped (station-gated journey); `:app:assembleDebug` APK 18,462,388 bytes sha256 `57a3f664…`; field journey 16 steps: 5 REAL / 8 DETERMINISTIC / 2 SYNTHETIC / 1 UNAVAILABLE-ON-STATION), PROD-015 declaration (final SHA `91f1b449…`, deployment `dpl_9G5gsVbGzpbWGBewyQUE4QhgadFY`, https://aise-tan.vercel.app), and `docs/productization-evidence/PROD-032/physical-device-lane.md` (documented lane, NOT YET EXECUTED — no results claimed there, none claimed here).

## Section A audit (deterministic code-level lane; every row cites file:line at HEAD 6a223f9)

All paths relative to `apps/android/`.

| # | Checklist item | Verdict | Evidence (file:line) |
|---|---|---|---|
| 1 | **Install/launch** | **PASS (deterministic)** | `applicationId "org.payswap.aise.app"` — `app/build.gradle.kts:23`; `compileSdk 35` (:20), `minSdk 26` (:29), `targetSdk 35` (:30), `versionCode 1`/`versionName "0.1.0"` (:31-32), `buildToolsVersion "35.0.0"` (:68); JDK 21 toolchain auto-provisioned via foojay — `settings.gradle.kts:23-27`, `app/build.gradle.kts:71-75`. Launcher activity `.MainActivity`, MAIN+LAUNCHER, `exported="true"` — `app/src/main/AndroidManifest.xml:32-41`; single activity, Compose-only (`MainActivity.kt:14-24`). Build types: only a `release` block (`isMinifyEnabled=false`, **no signing config**) — `app/build.gradle.kts:36-44` → a release APK would be unsigned/not installable; **the lane variant is debug** (`assembleDebug`), exactly what the station trio and PROD-032 lane doc §1 build. A debug APK IS installable on real hardware (station produced the same artifact: 18.4 MB). Default sync target baked at build time: `AISE_API_BASE_URL` env → BuildConfig, default `https://aise-tan.vercel.app` — `app/build.gradle.kts:24-28`. Launch smoke exists as instrumented test but is emulator-scoped (`app/src/androidTest/.../MainActivityInstrumentedTest.kt:26-32`). |
| 2 | **Permissions** | **PASS (deterministic)** | Manifest declares exactly CAMERA + INTERNET — `AndroidManifest.xml:14-15`; camera feature `required="false"` (camera-less devices install) — :20-22; `allowBackup="false"` (:26). NO RECORD_AUDIO (video is visual-only — `CameraCaptureAdapter.kt:48-49,171`), NO location, NO storage/media (app-private `<filesDir>/aise/` — `AppContainer.kt:22-23,44,52-53`). Runtime request is **mission-scoped from the capture screen**: state check + `RequestPermission` launcher — `CaptureScreen.kt:78-85`; request button "Grant camera access" :123; **denial path degrades honestly, no crash**: the capture stage is simply not composed; the honest card persists ("Camera permission required… No other permission is requested: no location, no microphone, no network.") — `CaptureScreen.kt:108-127`; permission re-checked on recomposition so an OS-Settings grant is picked up (:78-85); adapter binds only after the UI check (`CameraCaptureAdapter.kt:89` — MissingPermission suppressed because "permission is checked by the UI before binding"). Camera-bind failure also degrades honestly (`runCatching` → "Camera not available yet…") — `CaptureScreen.kt:158-165,205-211`. Grant/deny both-orders execution is runbook §3 (physical lane). |
| 3 | **Still/video capture** | **PASS (deterministic) + 1 minor GAP** | CameraX wiring: triple bind Preview+ImageCapture+VideoCapture (LIMITED+ guaranteed combination) — `CameraCaptureAdapter.kt:99-103`; on refusal, honest degrade to preview+still with `videoSupported=false` (:104-111), surfaced in UI as "Video not supported by this camera combination" — `CaptureScreen.kt:266-286`. Still path: `ImageProxy` JPEG bytes + metadata (`capture.kind`, `acquisition.sensorId`, `camera.orientation.degrees`, `camera.frameTimestampNanos`, latched exposure keys) — `CameraCaptureAdapter.kt:128-159,242-247`; exposure latched via Camera2Interop session-capture callback with keys that say `.latched` — :210-230 (honest provenance). Video path: Recorder writes into the writer's tmp file, no audio, Finalize event → `onFinalized(ok, durationNanos)` — :166-184; UI wires Record/Stop state-driven — `CaptureScreen.kt:167-179,266-279`. Output locations: `<filesDir>/aise/sessions/<sessionId>/assets/a-NNNN.jpg|.mp4`, tmp at `tmp/<assetId>.<ext>.tmp` (`SessionDirectory.kt:15,89-90`); file naming `a-%04d` — `CaptureSessionController.kt:433-436`. Crash-safe commit: hash-while-write → journal append (fsync, commit point) → atomic rename → store append — `CaptureSessionController.kt:241-293` (still, :254-279) and :300-372 (video chunked hash :331-340, journal+rename :366-367). Failure surfaces: still errors → `onError` callback, session continues (`CameraCaptureAdapter.kt:155-157`); video unusable → discard, nothing journaled (`VideoAssetWriter.discard` — `CaptureSessionController.kt:479-480`). **GAP-2 (minor, UX honesty)**: the screen's still-capture `onError` callback is EMPTY — `CaptureScreen.kt:258` (`onError = { }`) — a failed still produces NO operator-visible message (session/journal stay honest — nothing is journaled — but the failure is silent). Proposed governed fix: route the error into `viewModel.message` like every other illegal operation (same file, same pattern as `guarded()` messaging). |
| 4 | **Sensor metadata** | **PASS (deterministic)** | Rotation-vector capture: `TYPE_ROTATION_VECTOR` listener while the capture screen is visible — `RotationVectorSnapshotter.kt:26-45`; snapshot keys recorded with each capture: `sensor.rotation.x/.y/.z/.accuracy/.timestampNanos` — :57-63; **absence is absence, never a fabricated 0** (empty snapshot when no sensor/no reading) — :23-24,48-49. Attached to every still (`metadata + sensors.snapshot()` — `CaptureScreen.kt:254-257`) and to every video close (`sensors.snapshot() + video.durationNanos` — :170-174). Format: verbatim EXIF-ish open string map into `CapturedAssetRecord.sensorMetadata` (`AcquisitionMetadata`) — `CapturedAssetRecord.kt:45-46`, immutable + deterministically ordered. **Preservation through submission**: manifest exporter emits per-asset `acquisitionMetadata` verbatim (mission.id/session.id/device.id + sensor map) — `SessionManifestExporter.kt:58-82`; the transport's `canonicalEnvelope` carries `acquisitionMetadata` per wire asset — `HttpEvidenceSubmissionTransport.kt:139-144`; the envelope rides the SyncBatch POST `/v1/capture/sync` — :75-89. Metadata also enters the AISE-CONTENT-V1 contentId (identity over payload+metadata) — `CaptureSessionController.kt:262-263`. The `imuActive` device fact (rotation sensor presence, `AiseApplication.kt:40-41,52`) feeds the honest session capability baseline (`CaptureSessionController.kt:170-183`) — contrast: station records `imuActive=false`; hardware should record `true`. |
| 5 | **Pause/resume** | **PASS (deterministic)** | Semantics: journaled `SessionStateChanged` transitions CAPTURING⇄PAUSED — `CaptureSessionController.kt:185-187,214-229`; pause refused while a video segment is open (:220-222); finalize legal from CAPTURING/PAUSED/DRAFT (:194-201,198-199); assets only while CAPTURING (:425-431); one open session per device (:139-141). What persists: the journal (append-only, fsynced) — `JsonlSessionJournal.kt:80-93`; the UI state is DERIVED by replay (`transition` re-folds and republishes — `CaptureSessionController.kr:225-227` — file `CaptureSessionController.kt:225-227`). What the UI restores: status line per state ("Session <id8> — capturing/paused…") — `CaptureScreen.kt:189-203`; Pause/Resume/Finalize controls (:233-247); full session summary card (assets, intact/corrupted counts, recovery-event count) — :297-299,529-565. Rotation/backgrounding: camera use-cases are lifecycle-bound (`CameraCaptureAdapter.kt:90-113`) and the journal survives; the fold is the restore path. |
| 6 | **Process restart** | **PASS (deterministic)** | Startup recovery runs in background before UI reads state — `AiseApplication.kt:56-61` (`recoverOnStartup`, failure logged, never crashes app start). Exactly-once reopen: interrupted session re-opened via a `session.reopened` journal event; a second crash before activity sees the reopen tail and does NOT re-open — `SessionRecovery.kt:125-137` (ALREADY_REOPENED :126-128), audit + marker append :190-198. Asset integrity ladder: journaled assets verified cheaply (size + sha256 of first 64 KiB head sample) → full re-derivation of the content id → tmp ADOPTION for interrupted commits (crash between journal and rename) → explicit `asset.corrupted` (FILE_MISSING / CONTENT_ID_MISMATCH) with exclusion from the manifest — :158-180,215-248; uncommitted tmp files discarded and recorded (:184-188). Closed sessions: manifest re-derived if missing/stale (deterministic function of the journal) — :288-293. Journal crash discipline (WAL): one line + newline + fsync per append; torn tail = uncommitted append, discarded on read and truncated on next append (self-heal); complete-but-garbage line anywhere = hard corruption, fail closed — `JsonlSessionJournal.kt:14-24,46-71,102-130`. **Replay is deterministic**: pure fold validating strict +1 sequencing, legal transitions, assets-only-while-capturing, exactly-once reopens, monotonic time; fails closed with `JournalCorruptionException` (README §journal, `:99-104`; `SessionReplay.kt`; pinned by `SessionReplayDeterminismTest`, `SessionRecoveryTest`, `CaptureSessionControllerTest`). What survives `am force-stop`: everything journaled (session events, assets, reopen marker); what is lost: only the uncommitted tmp bytes that were never evidence. UI shows "Recovery events: N" — `CaptureScreen.kt:545`. |
| 7 | **Sync** | **PASS (deterministic) + 1 GAP (stale user-facing copy)** | Server submission path: real HTTP transport IS WIRED in the composition root — `AppContainer.kt:83-94` (`HttpEvidenceSubmissionTransport(baseUrl = BuildConfig.AISE_API_BASE_URL, sessionToken = authClient.sessionToken, sessionsRoot)`; base URL default `https://aise-tan.vercel.app` — `app/build.gradle.kts:24-28`, `AppContainer.kt:47-50`). Queueing offline: evidence persists as durable session journals/manifests; the submission engine models unavailability as a FIRST-CLASS `Deferred(reason, attempts)` state, probed via `/healthz` BEFORE every attempt — `FieldJourney.kt:122-138`; unauthenticated transport is honestly Unavailable("sign in to AISE on the mobile client before syncing evidence") — `HttpEvidenceSubmissionTransport.kt:23-34`; UI deferral banner renders the reason VERBATIM — `CaptureScreen.kt:472-480`. Retries: "Retry submission" re-attempts with the same payload/key — `FieldJourneyViewModel.kt:66-82`, `FieldJourney.resume` — `FieldJourney.kt:443-463`. **Idempotency**: deterministic key `aise-submission-v1:<sha256(payload)>` — `FieldJourney.kt:104-106`; server `ACCEPTED`/`DUPLICATE` both map to `Accepted(serverRef="session:<id>:sequence:0")` — `HttpEvidenceSubmissionTransport.kt:97-98`; partial uploads are impossible to double-count (assets re-uploaded content-addressed by raw sha256; server dedupes). Upload order: per-asset streaming upload `POST /v1/capture/assets/<rawSha>` (size + path-escape + re-hash verification first) then ONE atomic `POST /v1/capture/sync` SyncBatch with the idempotency key — :49-89,151-167 (checks: path escape :60, byte-size drift :62, re-derived sha :63, upload error → typed rejection :65-67). Partial upload: any asset failure aborts before the batch with a typed `Rejected(reason)`; server rejections surface reasonDetail/reasonCode verbatim — :90-108. **No path silently drops evidence**: corrupted assets are excluded from the manifest but explicitly recorded in the `recovery` audit — `SessionManifestExporter.kt:23-29,96-103`; `SubmissionFailed` renders the reason — `CaptureScreen.kt:490-492`; local `FINALIZED→SYNCED` marking happens only after server acceptance — `FieldJourneyViewModel.kt:53-56`, `CaptureSessionController.kt:379-403`. Canonical envelope projection drops only local-only fields (`relativePath`, `recovery`) that the strict server decode rejects — `HttpEvidenceSubmissionTransport.kt:111-149` (empirically validated: the 2026-09-23 station 400 `schema_invalid` drift, caught and fixed at `6728c3b`). Documented limitation (not a defect): video assets are not appended to the ByteArray-based `LocalCaptureStore` (README :375-383) — the submission path reads them from the session dir, so videos ARE submitted; only store listing misses them. **GAP-1 (stale user-facing copy)**: the SubmissionPanel text claims "The sync transport is not wired in this build — submission defers…" — `CaptureScreen.kt:328-333` — and the mission panel says "until the AISE-030 sync transport lands" (:378-383); stale KDoc makes the same claim — `FieldJourneyRuntime.kt:40-46,119-123`, `AppContainer.kt:79-81`. The transport IS wired and works when signed in; the copy contradicts the behavior and will mislead a physical-lane operator. Proposed governed fix: a documentation/copy refresh work item (no semantic change; no worker patch by me). |
| 8 | **Post-work capture** | **GAP (planned scope; classified honestly, NOT worker-patched)** | There is **no distinct post-work capture entry point** in this build. Navigation has exactly 4 destinations (HOME/CAPTURE/SETTINGS/ABOUT) — `AppDestination.kt:13-17,24`; no deep links (profile declares deep-links `none` — `MobileFieldAdapterProfile.kt:53-56`; manifest has only the launcher intent filter — `AndroidManifest.xml:37-40`). Task/mission identity is **build-time provisioned and badged**: `ProvisionedJourneyDocs` (`task-field-provisioned-0001`, `mission-2026-000042`, `proj-7f3a2b`, `case-91ab`) — `FieldJourneyRuntime.kt:186-251`; UI badges "Mission … (provisioned)" — `CaptureScreen.kt:428`. What IS preserved: `missionRef` flows from the active journey into the session journal and manifest (`CaptureScreen.kt:224-228`; `CaptureSessionController.kt:147-156`; `SessionManifestExporter.kt:88`), and submission carries the envelope with missionRef — so evidence↔mission linkage survives the adapter boundary; what is MISSING is web-originated task identity, the web→mobile handoff, and the post-work return path to the web outcome loop. These are explicitly assigned to Wave R1 / POST-005 ("web → mobile task handoff; mobile task identity/deep-link continuation; … post-work capture return path" — plan §6 Wave 1 Worker 2, `docs/post-production-discoverability-and-device-validation-plan-2026-09-25.md` §"Wave 1"). Current return-to-web is product-level only (web surfaces the synced evidence); the X-journey continuity record is harness-scoped (PROD-033 x-runs). Proposed governed route: POST-005 (Wave R1), plus this wave's runbook §10 records the honest observation on hardware. |
| 9 | **Capability negotiation** | **PASS (deterministic)** | The explicit browser-vs-mobile distinction lives at the shared adapter boundary, consumed by both clients. Committed contract: `packages/adapter-contract` (ADAPTER_CONTRACT_VERSION 1.0.0) with distinct reference profiles `fixtures/capability/ClientCapabilityProfile.valid-browser.json` vs `ClientCapabilityProfile.valid-mobile-field.json` and cross-profile negotiation fixtures (browser-vs-mobile field-depth-capture). Web side declares `adapterKind "browser"` from `REFERENCE_BROWSER_PROFILE` — `apps/web/src/app/adapter-profile.ts:7,32,48-53`. Android side: Kotlin mirrors of the contract (`AdapterContractVersion`, `ClientCapabilityProfile`/`TaskCapabilityRequirements`/`CapabilityNegotiation`, faithful `negotiateCapabilities` — byte-pinned against all committed fixtures) — `core/.../adapter/CapabilityNegotiation.kt:1-33,60-78,96-122` (worst-of severity blocked>unknown>degraded>permitted :68-78; `unknown` ≠ `unsupported` :60-66; permitted modes EMPTY when blocked :104-105,108-111). The honest mobile-field declaration: `ADAPTER_KIND "mobile-field"` — `MobileFieldAdapterProfile.kt:65`; still+video kinds only, depth/lidar NEVER declared (device depth fact flows into limitations verbatim) :84-111; IMU-derived sensor facts, GPS never declared :113-117; `persistent-store` with NO guessed byte bound :44-49; in-app notifications, deep-links `none` :50-56. Journey wiring: `assess` routes BLOCKED to an explicit phase — `FieldJourney.kt:369-380`; mission directives carry server instructions/mandatory/requirementRefs VERBATIM (:168-190,223-283); the UI renders blocked reasons and degraded notes VERBATIM (never a generic prompt) — `CaptureScreen.kt:405-409,412-425,511-527`; degradation changes operator burden, never assurance thresholds (asserted by the committed `:core` suites). |

**Section A summary: 7 PASS (code-level proof cited), 2 GAP rows on checklist semantics (GAP-1 stale submission-panel copy `CaptureScreen.kt:328-333` with governed fix = copy refresh; GAP-2 empty `onError` at `CaptureScreen.kt:258` with governed fix = route to ViewModel message), plus checklist item 8 classified GAP-by-planned-scope (POST-005). No N/A rows.** All Section A rows are DETERMINISTIC evidence (code-level audit) — none of it is device evidence, and it is not classified as such.

## Section B runnable lane (time-boxed, honestly classified)

- Environment probe (exact outputs): `java -version` → `openjdk version "21.0.12.1" 2026-08-18` (JRE, Debian); `javac` → NOT FOUND; `gradle`, `adb`, `sdkmanager`, `emulator`, `kotlinc` → NOT FOUND; `/dev/kvm` → NOT PRESENT; 2 CPUs / 4.1 GB RAM / 7.9 GB free disk.
- Exact command: `cd /home/z/AISE/apps/android && timeout 1200 ./gradlew :core:test --no-daemon` (launched 2026-09-26T00:40:24Z under nohup with a full transcript at `/home/z/post002-jvm-lane.log`).
- Outcome: the Gradle wrapper began downloading `gradle-8.14.3-bin.zip` (~138 MB; `gradle/wrapper/gradle-wrapper.properties` pins 8.14.3) and accumulated only ~13 MB inside the 20-minute hard box — the route to `services.gradle.org` is throttled to ~11 KB/s in this sandbox (by contrast, the GitHub clone of the full repo completed in seconds). The `timeout 1200` terminated the wrapper at 01:00:24Z **before any Gradle execution**: no daemon/configuration output, no `core/build/` directory, no test XML, `~/.gradle` cache stuck at 13 MB, no lingering java processes. The harness shell was killed together with its child, so its own `EXIT CODE` echo was not flushed; the observable termination is the timeout kill (124-class). Recorded verbatim from the log and process observation — nothing embellished.
- Test counts: **NONE claimed** — zero tests executed. The prior station's `:core:test` 34 suites/387 tests/0 failures at `6728c3b` is EMULATED evidence (committed PROD-032 transcript), cited, not re-claimed; `apps/android` is byte-identical at HEAD (see Section D), so that record remains structurally applicable, but it is still not a fresh run.
- Classification: **BLOCKED_NO_TOOLCHAIN** — contributing facts: JRE-only (no `javac`), no pre-installed Gradle, no dependency cache, and a network route that cannot deliver the Gradle distribution inside the mandated 20-minute box. The abort was clean (no partial state inside the repo; clone working tree remains clean).

## Section C artifacts

Written to disk at `/home/z/post002-artifacts/` (outside the clone — read-only wave preserved) and reproduced complete below.

### C-1. `PHYSICAL-DEVICE-RUNBOOK.md`

````markdown
# AISE POST-002 — PHYSICAL-DEVICE RUNBOOK (Wave R0, Worker 2)

Operator procedure for the physical Android device lane at repo HEAD
`6a223f9fe74ea94138c4b62db8b228a615522e01` (live main at dispatch; `apps/android`
is byte-identical to the PROD-032 station delivery SHA `6728c3b`, so all station
gradle-trio references apply unchanged).

App facts (code-cited): applicationId `org.payswap.aise.app`
(`app/build.gradle.kts:23`), minSdk 26 / targetSdk 35 / compileSdk 35
(`app/build.gradle.kts:20,29,30`), versionName 0.1.0 / versionCode 1
(`app/build.gradle.kts:31-32`), single launcher activity `.MainActivity`
(`AndroidManifest.xml:32-41`), permissions CAMERA + INTERNET only, camera
feature `required="false"` (`AndroidManifest.xml:14-22`), debug APK ~18.4 MB
(station reference: 18,462,388 bytes, sha256 `57a3f664…` at `6728c3b`).

**Classify every step per EVIDENCE-CLASSIFICATION-PROTOCOL.md. Every step below
must produce an artifact (screenshot / photo / screen recording / logcat
excerpt / curl output / sha256sum). Absence of hardware for any step is an
honest BLOCKED row — never skipped silently.**

---

## 0. Prerequisites

- Physical Android device, API 26+ (Android 8.0+), USB debugging enabled.
  Record BEFORE starting: `adb shell getprop ro.product.model`,
  `adb shell getprop ro.build.version.release`,
  `adb shell getprop ro.build.fingerprint`.
- Workstation: git, JDK 21 (Temurin), Android SDK (`platforms;android-35`,
  `build-tools;35.0.0`, `platform-tools`/adb; accept licenses). The Gradle
  JDK 21 toolchain auto-provisions on JRE-only machines via the foojay
  resolver (`apps/android/settings.gradle.kts:23-27`).
- Decide the SYNC TARGET (two lanes; record which one you ran):

  | lane | target | requirement |
  |---|---|---|
  | **A (default, recommended)** | `https://aise-tan.vercel.app` | nothing — this is the baked-in default (`app/build.gradle.kts:24-28`); device needs internet |
  | **B (loopback)** | `http://127.0.0.1:8791` (local `backend/api`) | build-time override + debug cleartext overlay (§0.1) + `adb reverse` |

### 0.1 Loopback-lane honest gap (BLOCKS plain HTTP on the device — by design)

The APK ships NO `usesCleartextTraffic` and NO `networkSecurityConfig`
(verified by grep over `apps/android` at HEAD — zero matches). Android 9+
(API 28+) blocks plain-HTTP to `127.0.0.1` from an app without a cleartext
grant. Also NOTE: **the app has NO runtime server-URL override** — Settings
only *displays* the build-time URL (`SettingsScreen.kt:64`,
`AppContainer.kt:47`); the PROD-032 lane doc's "Settings → server base URL"
instruction is stale against this build. For lane B you must therefore:

1. Create `apps/android/app/src/debug/AndroidManifest.xml` (debug-variant
   overlay only — never ships in release):
   ```xml
   <manifest xmlns:android="http://schemas.android.com/apk/res/android">
       <application android:usesCleartextTraffic="true" />
   </manifest>
   ```
2. Build with the base URL override:
   `AISE_API_BASE_URL="http://127.0.0.1:8791" ./gradlew :app:assembleDebug`
3. After install: `adb reverse tcp:8791 tcp:8791` (device
   `127.0.0.1:8791` → workstation).
4. Boot the real backend on the workstation exactly as the station does
   (`apps/android/scripts/e2b-station/field-journey.sh:66-97`):
   `bun install` at repo root, then in `backend/api` with
   `AISE_AUTH=1 AISE_AUTH_MODE=demo-open AUTH_SECRET=<random 32B hex>
   HOST=127.0.0.1 PORT=8791 AISE_DATA_DIR=<disposable dir> bun run start`;
   wait for `curl -fsS http://127.0.0.1:8791/healthz` → 200.

## 1. Build the APK exactly as the station does

```bash
git clone https://github.com/payswapdotorg/AISE.git && cd AISE
git rev-parse HEAD          # record: expect 6a223f9fe74ea94138c4b62db8b228a615522e01 (or the live HEAD you audited)
cd apps/android
./gradlew :core:test :app:test :app:assembleDebug   # the station gradle trio
sha256sum app/build/outputs/apk/debug/app-debug.apk # record
```

Expected: `:core:test` 34 suites / 387 tests / 0 failures; `:app:test` 176
tests / 0 failures / 2 skipped (the station-gated journey test; PROD-032 §2);
APK ~18.4 MB. Any divergence → STOP and record it as a defect, do not proceed
on a red build.

**Evidence:** full command transcript + `sha256sum` output + test-count
summary (per-suite XML under `core/build/test-results/test/`,
`app/build/test-results/testDebugUnitTest/`). Class: deterministic.

## 2. Install and launch

```bash
adb devices                                                # device visible + authorized
adb install -r apps/android/app/build/outputs/apk/debug/app-debug.apk
adb shell dumpsys package org.payswap.aise.app | grep -E 'versionName|targetSdk|permission'
adb shell monkey -p org.payswap.aise.app -c android.intent.category.LAUNCHER 1   # or tap the icon
```

Expected observable: install succeeds; `versionName=0.1.0`; app launches to
the Home tab with bottom navigation Home / Capture / Settings / About
(`AiseApp.kt:45-61`). No crash dialog.

**Evidence:** `adb devices` output, `dumpsys` grep, launch screenshot, first
30 s of `adb logcat` (filter `AndroidRuntime:E`). Class: physical.

## 3. Runtime permission matrix (grant/deny, BOTH orders)

The CAMERA permission is requested **mission-scoped from the capture screen**
(`CaptureScreen.kt:78-85,108-130`), never at install/launch. The manifest
declares only CAMERA + INTERNET; video records NO audio (no RECORD_AUDIO —
`CameraCaptureAdapter.kt:171`); no storage/media permission (app-private
`<filesDir>/aise/`).

Run BOTH orders and record each:

- **Order 1 (grant):** open Capture tab → observe the "Camera permission
  required" card BEFORE granting (screenshot) → tap "Grant camera access" →
  OS dialog appears at capture time (screenshot/video of the dialog) → Allow
  → preview renders, capture controls appear.
- **Deny once:** Deny the OS dialog → the card persists with the honest copy
  ("Capture needs the camera while a session is active. No other permission
  is requested…") → NO crash, NO force-close (`adb logcat` clean of
  `AndroidRuntime` crashes) → app remains navigable.
- **Order 2 (deny-first, then grant via Settings):** fresh install or
  `adb shell pm reset-permissions` → Deny twice ("Don't ask again" state if
  offered) → the card remains (the honest degraded state) → navigate to OS
  Settings → Apps → AISE → Permissions → Camera → Allow → return to the app
  → the capture stage now renders (permission state is re-checked on
  composition, `CaptureScreen.kt:78-85`).
- Record granted state:
  `adb shell dumpsys package org.payswap.aise.app | grep -A6 'runtime permissions'`

**Evidence per sub-step:** screenshot + logcat excerpt + `dumpsys` output.
Class: physical.

## 4. Capability assessment + session start

On the Capture tab: tap **"Start field journey"**. Expected: the mission
panel renders the negotiated verdict banner (expect `Task permitted` on a
camera+IMU device) and the 3 provisioned mission steps with exact capture
actions ("Capture still image … Crack pattern overview stills (mandatory)",
"… detail stills (mandatory)", "Record video footage … (optional)") and
"Evidence gap — not yet captured" per step. NOTE honestly: mission/task
documents are BUILD-TIME PROVISIONED and badged "(provisioned)" in the UI
(`ProvisionedJourneyDocs`, `FieldJourneyRuntime.kt:186-251`,
`CaptureScreen.kt:428`) — live server task fetch is not in this build.

Then tap **"Start session"** (missionRef flows from the active mission,
`CaptureScreen.kt:222-232`). Expected: status line "Session <id8> —
capturing". On a device WITH a rotation-vector sensor the session baseline
records `imuActive=true` (contrast the station's honest `imuActive=false`).

**Evidence:** screenshots of verdict banner + step list + session status.
Class: physical (device capability facts) + deterministic (journey state
machine — classify the verdict banner row as physical-sourced since the
profile derives from THIS device's hardware facts).

## 5. Guided still capture

Tap **"Still"** (enabled while CAPTURING and not recording). Expected:
message "Still captured: a-0001 (<N> bytes)"; the session summary lists
`a-0001 — image/jpeg, N bytes`; the mission step's gap flips to "Evidence
recorded: 1 asset(s)".

Record: file lands at
`/data/data/org.payswap.aise.app/files/aise/sessions/<sessionId>/assets/a-0001.jpg`
(app-private). Pull for verification:

```bash
adb shell run-as org.payswap.aise.app ls files/aise/sessions/
adb shell run-as org.payswap.aise.app cat files/aise/sessions/<id>/manifest.json   # after finalize (§8)
```

**Sensor-metadata verification (what to look for):** after finalize, in the
manifest's `assets[].acquisitionMetadata`, expect EXACTLY these verbatim
keys — `capture.kind=still`, `acquisition.sensorId=<cameraId>`,
`camera.orientation.degrees`, `camera.frameTimestampNanos`,
`camera.exposureTimeNs.latched`, `camera.iso.latched`,
`camera.focalLengthMm.latched`, `camera.exposure.latchedAtSensorNanos`
(latched keys only after the first preview frames; keys say `.latched` —
honest provenance, `CameraCaptureAdapter.kt:142-147,242-247`),
`sensor.rotation.x/.y/.z/.accuracy/.timestampNanos`
(`RotationVectorSnapshotter.kt:57-63`), plus `mission.id`, `session.id`,
`device.id` (`CaptureSessionController.kt:438-442`). Cross-check: capture
one still in landscape and one in portrait → `camera.orientation.degrees`
and/or `sensor.rotation.*` values MUST CHANGE (non-fabricated evidence). On
a device WITHOUT a rotation-vector sensor: the sensor keys are ABSENT (empty
snapshot — "absence is absence, never a fabricated 0",
`RotationVectorSnapshotter.kt:23-24,48-49`) — record that honestly; it is
valid evidence.

**Evidence:** screenshot after each still; the manifest JSON (post-finalize)
with the metadata keys; byte sizes. Class: physical.

## 6. Guided video capture

Tap **"Record"** → recording indicator (Stop button appears) → hold ≥10 s →
tap **"Stop recording"**. Expected: message "Video captured: a-0002
(<N> bytes)"; summary lists `a-0002 — video/mp4, N bytes`; file at
`…/assets/a-0002.mp4`. The segment carries `video.durationNanos` in its
metadata (`CaptureScreen.kt:170-174`). NOTE: video has NO audio track by
design (no RECORD_AUDIO; `CameraCaptureAdapter.kt:171`). If the device
refuses the Preview+ImageCapture+VideoCapture triple binding, the UI shows
"Video not supported by this camera combination" and hides Record — that is
the honest degraded fact (`CameraCaptureAdapter.kt:99-111`,
`CaptureScreen.kt:266-286`); record it if it occurs.

**Evidence:** screen recording of the whole capture (see §11); manifest
metadata incl. `video.durationNanos`; file size. Class: physical.

## 7. Pause / resume (+ mid-session permission/process checks)

While CAPTURING with ≥1 asset: tap **"Pause"** → status "Session <id8> —
paused" → tap **"Resume"** → status returns to "capturing". Capture a still
while paused is REFUSED (the Still button is disabled while paused — assets
are only legal while CAPTURING, `CaptureSessionController.kt:425-431`).
Also: rotation/backgrounding — rotate the device and background/foreground
the app mid-session: the session survives (journal is the truth; camera
rebinds with lifecycle).

What persists: every transition is a journaled `session.state` event
(`CaptureSessionController.kt:214-229`); the UI restores purely from the
journal fold. Expected observable after process death is §8.

**Evidence:** screenshots of each status; screen recording of
pause→resume→capture. Class: physical.

## 8. Process restart (force-stop + relaunch)

```bash
adb shell am force-stop org.payswap.aise.app
adb shell monkey -p org.payswap.aise.app -c android.intent.category.LAUNCHER 1
```

Expected: the Capture tab shows the SAME session id in status
"Session <id8> — capturing" (reopened by recovery); the session summary card
shows **"Recovery events: 1"** (`CaptureScreen.kt:545`); all previously
captured assets still listed and intact; a `session.reopened` event was
appended exactly once (`SessionRecovery.kt:125-137,198`). Repeat the
force-stop WITHOUT any operator action in between → relaunch → still
"Recovery events: 1" (exactly-once: the second recovery does NOT re-open;
`SessionRecovery.kt:126-128`).

Then: pause the session → force-stop → relaunch → status "paused" restored
(journaled state survives).

**Evidence:** screenshots after each relaunch (session id + recovery count),
logcat excerpt. Class: physical (process death is real) with the recovery
SEMANTICS verified deterministically by the committed
`SessionRecoveryTest`/`CaptureSessionControllerTest` suites.

## 9. Finalize + submit-blocked + submit + idempotent resubmit + SYNCED

1. **Finalize:** tap **"Finalize"** → message "Session finalized — manifest:
   manifest.json"; status "Session finalized"; the mission panel's submission
   panel appears ("Evidence submission" card). The manifest is the
   deterministic journal projection
   (`CaptureSessionController.kt:194-212`).
2. **Submit-blocked (unauthenticated):** Settings tab → (if signed in)
   "Sign out" → Capture tab → "Submit evidence" → expected: the explicit
   deferral banner — `deferred-offline` with reason "sign in to AISE on the
   mobile client before syncing evidence"
   (`HttpEvidenceSubmissionTransport.kt:24-26`). NO crash, NO silent drop:
   evidence stays in the durable offline store.
3. **Submit:** Settings → "Enter demo" (lane A: the deployed server; lane B:
   the loopback backend) → signed-in message with principal name → Capture →
   "Submit evidence". Expected: "Submitted — server ref
   session:<sessionId>:sequence:0; local session marked synced"; the verdict
   banner flips to `submitted`; the local journal gains FINALIZED→SYNCED
   (`FieldJourneyViewModel.kt:53-56`,
   `CaptureSessionController.kt:379-403`).
4. **Idempotent resubmit:** tap "Retry submission" (or Submit again after
   reset+re-journey): the same idempotency key (`aise-submission-v1:<sha256
   of payload>`, `FieldJourney.kt:104-106`) is re-sent; the server answers
   DUPLICATE and the client treats it as Accepted
   (`HttpEvidenceSubmissionTransport.kt:97-98`). NO duplicate evidence.
5. **Server verification (workstation):**
   ```bash
   # lane A:
   curl -fsS https://aise-tan.vercel.app/healthz
   # lane B:
   curl -fsS http://127.0.0.1:8791/healthz
   # session id = the app's session id (from status line / sync message):
   curl -fsS -H "Accept: application/json" <BASE>/v1/capture/sessions/<session-id> | head -c 800
   ```
   Expected: HTTP 200 with the session envelope (the sequence the device
   submitted). Lane A alternative: sign in on the web app and locate the
   evidence/capture-session record in the product UI (see §10).

**Evidence:** screenshots of every banner/message; curl outputs; logcat.
Class: physical (real device transport) — the server-side acceptance is REAL
network evidence; classify the row "physical (device-originated, real
server answer)".

## 10. Post-work capture entry point + return-to-web (HONEST current state)

- **Current build fact:** there is NO distinct post-work-capture entry
  point. Navigation has exactly 4 destinations
  (`AppDestination.kt:13-17`); the field journey's task/mission identity is
  build-time provisioned (`ProvisionedJourneyDocs`,
  `FieldJourneyRuntime.kt:186-251`); no deep links are declared
  (profile: deep-links `none`, `MobileFieldAdapterProfile.kt:53-56`). The
  web→mobile handoff, post-work capture return path and task-identity
  continuity are assigned to Wave R1 / POST-005 (plan §6 Wave 1 Worker 2).
- **What the operator records instead (this wave):** after §9, on a DESKTOP
  browser open `<BASE>` (lane A: https://aise-tan.vercel.app), sign in with
  the same demo principal, and verify the synced session/evidence is visible
  in the web product (evidence/capture surfaces). Screenshot the web-side
  record showing the device session id. This is the CURRENT combined-loop
  continuity check at product level; the full X-journey (field→office with
  preserved task/evidence identity) is PROD-033's harness-scoped record plus
  this manual check, honestly labeled.
- **Deliverable for the Lead:** file the absence as a confirmed GAP row
  (defect: "no post-work capture entry point / no task-identity return path
  in the Android build") with the file:line citations above and the proposed
  governed fix route (POST-005 scope), NOT a worker patch.

**Evidence:** web screenshot(s) + the device session id cross-reference.
Class: physical (device-originated evidence appearing in the web product) —
but the *entry-point absence* is a deterministic code-level fact.

## 11. Evidence capture commands (run alongside §2–§10)

```bash
adb shell getprop ro.build.fingerprint                       # device fingerprint
adb shell getprop ro.product.model; adb shell getprop ro.build.version.release
adb shell screenrecord /sdcard/aise-post002.mp4 &            # start BEFORE §3; stop after §10
adb pull /sdcard/aise-post002.mp4 .
adb logcat -d > aise-post002-logcat.txt                      # after each numbered section, or at the end
adb shell dumpsys package org.payswap.aise.app > aise-post002-package.txt
sha256sum app-debug.apk aise-post002.mp4 aise-post002-logcat.txt
```

Photos of the device screen are ALSO acceptable first-class evidence
(physical class) where screenrecord is impractical.

## 12. Filing

File under `docs/productization-evidence/PROD-032/physical-device/` (per the
committed lane doc §5) or a new dated evidence directory per the Lead's
instruction, using EVIDENCE-RECORD-TEMPLATE.md per run: fingerprint, journey
recording, logcat, session GET output, per-step classification table updated
to physical fidelity. **Until a real run fills it in, physical fidelity
remains UNEVIDENCED — by design, honestly.**

## Known operator-facing defects this audit found (do NOT patch in the lane;
record them as observed)

1. Submission-panel copy says "The sync transport is not wired in this
   build…" (`CaptureScreen.kt:328-333`) — STALE: the HTTP transport IS wired
   (`AppContainer.kt:83-94`). Expect actual submission to WORK while the
   panel text claims it will defer. Record the contradiction.
2. Still-capture failures are swallowed by an empty `onError` callback
   (`CaptureScreen.kt:258`) — if a still fails you will see NO message.
   Record any silent failure with logcat.
3. PROD-032 lane doc §3's "Settings → server base URL" instruction is stale
   (no runtime override exists — `SettingsScreen.kt:64` is read-only).
````

### C-2. `EVIDENCE-CLASSIFICATION-PROTOCOL.md`

````markdown
# AISE POST-002 — EVIDENCE CLASSIFICATION PROTOCOL

Binding rules for classifying every step of the Android/device validation
lanes. Derived from `docs/TECH-LEAD-HANDOFF.md` §8/§14/§15 and
`docs/post-production-discoverability-and-device-validation-plan-2026-09-25.md`
§4, and aligned with the class discipline already used by the committed
PROD-033 journey records.

## 1. The four evidence classes

The class is EXACTLY ONE of {**deterministic**, **synthetic**, **emulated**,
**physical**} and it names the EVIDENCE SOURCE — where the proof bytes came
from — never the execution mode alone. The parenthesized qualifier names the
execution mode. Classes are never upgraded, never re-labeled after the fact,
and never dropped.

| class | means | examples |
|---|---|---|
| **deterministic** | proof produced by pure code execution with no environment variance: unit suites, pure state machines, schema validation, replay folds, code-level audit citing file:line | `:core:test` 387 tests on a JVM; `SessionReplay` fold; journal determinism pins; this POST-002 Section A audit |
| **synthetic** | inputs/artifacts generated in-test or in-harness — real code paths exercised on fake evidence bytes | station journey's 12 KB deterministic still / 24 KB video bytes; generated fixtures |
| **emulated** | evidence recorded on a non-physical execution environment (E2B sandbox, Android emulator, committed transcripts cited forward) | the PROD-032 station Gradle trio + field journey (committed transcripts at `6728c3b`); any emulator run (if one ever boots) |
| **physical** | proof produced by real hardware in the operator's hands: real camera frames, real IMU readings, real OS permission dialogs, real process death on a device | the lane this runbook prepares; photos/screen recordings of the physical device; real device network sync |

## 2. The never-upgrade law (verbatim from the work order and Handoff §8/§14)

1. **Never upgrade one class into another.** Emulator results are never
   physical evidence; a code-level audit is deterministic evidence, not
   device evidence; carried-forward committed transcripts are emulated even
   when their original fidelity labels were REAL (the carrying document
   records the source verbatim — PROD-033 m-run discipline).
2. **Never accept:** documentation-only claims as runtime evidence; emulator
   results as physical-device evidence; a later main SHA as covered by older
   deployment evidence.
3. **A live run is never fabricated.** If Chromium/hardware/emulator is
   unavailable, the step records its honest fallback (or BLOCKED) — the class
   column says so.
4. **Absence of hardware is an honest BLOCKED state, not a failure to hide.**
   BLOCKED rows (e.g. `BLOCKED_NO_KVM`, `BLOCKED_NO_PHYSICAL_HARDWARE`,
   `BLOCKED_NO_TOOLCHAIN`, `BLOCKED_NO_CREDENTIALS`) are recorded, never
   dropped, never counted as harness failures.
5. **A claim without a committed artifact behind it is not evidence.** Every
   PASS row cites its artifact (file path + digest where applicable).
6. **A fresh run you did not do is never claimed.** If no fresh run was
   performed (e.g. no E2B_API_KEY), the record states that VERBATIM.

## 3. Per-step classification rules for this lane

- Steps executed on the physical device (install, launch, OS dialogs, real
  camera still/video, real sensor readings, force-stop, device-originated
  HTTP sync): **physical**.
- Steps that are pure UI state machines whose semantics are pinned by
  committed JVM tests (journey phases, journal fold, recovery exactly-once):
  the OBSERVATION is physical; the SEMANTICS guarantee is deterministic.
  Record the row as `physical (semantics pinned by deterministic suite X)`.
- Server-side acceptance (HTTP 200 / ACCEPTED / DUPLICATE from a real
  backend reached from the device): `physical (device-originated, real
  server answer)`.
- The Gradle trio on the operator workstation: **deterministic** (same
  evidence class as on the station — the station's own emulated record of it
  does not transfer).
- Anything replayed from committed transcripts: **emulated**, with the
  original fidelity labels carried forward verbatim, never upgraded.
- Expected-but-not-observed capabilities (e.g. no rotation-vector sensor on
  the test device): the honest absence is recorded — class `physical`
  (a real hardware fact), content "absence observed and recorded".

## 4. Evidence record format (binding table template)

One table per run; one row per numbered runbook step. Verdict is
PASS / FAIL / BLOCKED_<REASON> — nothing else.

| step | action | observed | class | artifact | timestamp (UTC) |
|---|---|---|---|---|---|
| §2 | `adb install -r app-debug.apk` | install Success, versionName 0.1.0, launches to Home | physical | `02-install.png`, `aise-post002-logcat.txt:L12-40` | 2026-09-25T16:03:12Z |
| §5 | guided still capture (portrait + landscape) | a-0001 image/jpeg N bytes; `sensor.rotation.*` values differ between orientations | physical | `05-still.png`, manifest JSON | … |
| §8 | force-stop + relaunch ×2 | same session id; Recovery events: 1 after both | physical (semantics pinned by SessionRecoveryTest) | `08-recovery-1.png`, `08-recovery-2.png` | … |
| §n | … | BLOCKED_NO_ROTATION_SENSOR (device model has no rotation-vector sensor; honest absence) | physical | `0n-imu-absence.png` | … |

Rules: `observed` must be operator-verbatim (what was seen, including
unexpected results and the stale-copy contradiction in
`CaptureScreen.kt:328-333` if observed); `artifact` must name a file that is
actually committed/filed; `timestamp` is the observation instant, UTC.

## 5. Security/secret discipline for evidence artifacts

- No secrets in any artifact: no AUTH_SECRET values, no session tokens
  (tokens are in-memory only by construction — `MobileAuthClient.kt:24-25`
  — and never rendered in the UI), no API keys, no E2B keys. If a screen
  recording accidentally captures a secret, re-record; do not redact
  transcripts by hand.
- Camera/photo evidence excludes secrets by construction: manifests carry
  only content hashes, sizes, media types, acquisition metadata and device
  identity (`SessionManifestExporter.kt:58-105`); no principal PII beyond
  the display name the demo principal publishes.
- `adb shell run-as`/`dumpsys` outputs: review before filing (they can
  contain paths but no secrets in this app's private storage).
````

### C-3. `EVIDENCE-RECORD-TEMPLATE.md`

````markdown
# AISE POST-002 — EVIDENCE RECORD TEMPLATE (fill ONE per physical run)

Copy this file per run. Every field is mandatory; write `N/A` only with a
recorded reason. File the completed record with its artifacts under
`docs/productization-evidence/PROD-032/physical-device/` (or the dated
evidence directory the Tech Lead designates).

## Run header

| field | value |
|---|---|
| Run ID | POST-002-PHYSICAL-<YYYYMMDD>-<seq> |
| Date (UTC) | |
| Operator | |
| Operator role | (contractor / Lead / named worker — the Lead harvests and files) |
| Repo URL + SHA | https://github.com/payswapdotorg/AISE @ `git rev-parse HEAD` = |
| apps/android drift check | `git diff --stat 6728c3b..HEAD -- apps/android` output = |
| APK path + sha256 | app/build/outputs/apk/debug/app-debug.apk, sha256 = |
| APK byte size | |
| Sync lane used | A (https://aise-tan.vercel.app) / B (loopback 127.0.0.1:8791 + cleartext overlay + adb reverse) |
| Backend healthcheck output | (curl /healthz output + timestamp) |
| Build gates | :core:test = __ suites / __ tests / __ failures; :app:test = __ / __ / __ (skipped __); assembleDebug = PASS/FAIL |

## Device header

| field | value |
|---|---|
| Model (`ro.product.model`) | |
| Android release (`ro.build.version.release`) / API level | |
| Build fingerprint (`ro.build.fingerprint`) | |
| Cameras present (rear count) | |
| Rotation-vector sensor present (yes/no) | |
| Screen size class | |
| Granted permissions at end (dumpsys excerpt) | |

## Per-step evidence table (one row per runbook §; verdict ∈ PASS/FAIL/BLOCKED_<REASON>)

| step | action | observed (verbatim) | class (physical/deterministic/synthetic/emulated + qualifier) | artifact | timestamp UTC |
|---|---|---|---|---|---|
| §2 install/launch | | | | | |
| §3 permission order 1 (grant) | | | | | |
| §3 permission deny-once | | | | | |
| §3 permission order 2 (deny→settings→grant) | | | | | |
| §4 capability assessment + session start | | | | | |
| §5 still capture (portrait) | | | | | |
| §5 still capture (landscape; metadata changed?) | | | | | |
| §5 sensor metadata keys present/absent (list them) | | | | | |
| §6 video capture (incl. video.durationNanos) | | | | | |
| §7 pause → resume | | | | | |
| §8 force-stop + relaunch #1 (recovery events count) | | | | | |
| §8 force-stop + relaunch #2 (exactly-once) | | | | | |
| §8 paused-state survival | | | | | |
| §9 finalize (manifest written) | | | | | |
| §9 submit-blocked (signed out) — exact deferral reason | | | | | |
| §9 submit (server ref) | | | | | |
| §9 idempotent resubmit (DUPLICATE) | | | | | |
| §9 server-side session GET (curl output head) | | | | | |
| §10 web return-to-web check (session visible on web) | | | | | |
| §10 post-work-capture entry point (expected GAP observation) | | | | | |

## Defects/observations (including the three known audit findings, if observed)

| # | observation | file:line if code-related | severity | proposed governed route |
|---|---|---|---|---|
| 1 | stale submission-panel copy ("sync transport is not wired") | apps/android/…/CaptureScreen.kt:328-333 | minor/docs | text refresh in a governed UI work item |
| 2 | still-capture failure swallowed (empty onError) | apps/android/…/CaptureScreen.kt:258 | minor/UX | route error to ViewModel message |
| 3 | no post-work capture entry point / no task-identity return | AppDestination.kt:13-17, FieldJourneyRuntime.kt:186-251 | planned | POST-005 (Wave R1) scope |
| … | (new observations) | | | |

## Artifact manifest (every file + sha256)

| artifact | sha256 |
|---|---|
| aise-post002.mp4 (screen recording) | |
| aise-post002-logcat.txt | |
| aise-post002-package.txt | |
| screenshots (…) | |
| manifest.json (session envelope) | |
| server session GET output | |

## Honesty attestation

- [ ] Every PASS row cites a committed artifact.
- [ ] Every BLOCKED row states its reason verbatim and was not dropped.
- [ ] No evidence class was upgraded (no emulator/synthetic/deterministic row
      is labeled physical; deterministic code-audit rows are labeled
      deterministic).
- [ ] No secrets appear in any artifact (no AUTH_SECRET, no session token,
      no API keys; tokens are in-memory only by construction).
- [ ] A fresh run was actually performed by the named operator at the stated
      date/time on the stated device.

Operator signature/date: ____________________
Lead harvest/filing confirmation: ____________________
````

## Section D station check: **CURRENT** (one stale doc instruction cited) — lane class `BLOCKED_NO_CREDENTIALS`

- **No drift in the station's subject**: `git diff --stat 6728c3b..HEAD -- apps/android` is **EMPTY** — the entire Android project (app, core, scripts, wrapper) is byte-identical to the PROD-032 station delivery SHA `6728c3b` at live HEAD `6a223f9`. The station scripts therefore remain current by construction.
- Script-by-script verification against the app's actual structure:
  - `scripts/e2b-station/gradle-trio.sh:80-85` runs `:core:test`, `:app:test`, `:app:assembleDebug` and summarizes `core/build/test-results/test` + `app/build/test-results/test{Debug,Release}UnitTest` — all match the current module layout and Gradle tasks; APK path `app/build/outputs/apk/debug/app-debug.apk` (:88-90) matches.
  - `scripts/e2b-station/field-journey.sh:109` runs `:app:testDebugUnitTest --tests org.payswap.aise.app.field.FieldJourneyStationSyncTest` — the test exists and is gated on the same env vars the script exports (`FieldJourneyStationSyncTest.kt:63-71` reads `AISE_STATION_SYNC_BASE_URL` / `AISE_STATION_JOURNAL_PATH`); the backend boot block (:66-97) matches the current `backend/api` boot contract (`/healthz`, demo-auth env vars).
  - `scripts/e2b-station/bootstrap.sh:33,38` requires the pinned `AISE_REPO_SHA` and provisions `platform-tools`, `platforms;android-35`, `build-tools;35.0.0` — matches `app/build.gradle.kts:20,29-30,68`.
  - `scripts/e2b-station/emulator-probe.sh` (layered `/dev/kvm` → `-accel-check` → real AVD boot probe) unchanged; its recorded verdict class (`BLOCKED_NO_KVM` on KVM-less sandboxes) is exactly what this sandbox would produce.
  - `scripts/e2b-station/station-driver.py:37-51,106-110` enforces the E2B_API_KEY runtime-secret discipline (hard, loud error when absent; never echoed/logged).
  - `scripts/android-cli-station.sh` and `docs/android-cli-development-station.md` (CLI station, API 35 toolchain, no Android Studio) are consistent with the current app structure.
- **Backend drift is irrelevant to the station journey's surfaces**: since `6728c3b`, `backend/api` gained 13,916 lines (provider-scorecard, solution routes, `runtime/entry.ts` +70) but `backend/api/src/capture/*` and `backend/api/src/auth` are **unchanged** (empty diffs) — the routes the app and station journey exercise (`/healthz`, `/v1/auth/demo`, `/v1/capture/assets/*`, `/v1/capture/sync`, `/v1/capture/sessions/*`) are untouched.
- **Drift found (documentation only, one item)**: `docs/productization-evidence/PROD-032/physical-device-lane.md` §3 (lines 65-67) instructs the operator: "In the app: **Settings → server base URL = `http://127.0.0.1:8791`**" — the current app has NO runtime base-URL setting; Settings only displays the build-time URL (`SettingsScreen.kt:64`, read-only `Text`; `AppContainer.kt:47` = `BuildConfig.AISE_API_BASE_URL`, overridable only at build time via `AISE_API_BASE_URL` — `app/build.gradle.kts:24-28`). The runbook §0.1 supersedes this instruction. Proposed governed fix: refresh that one lane-doc paragraph when the Lead files this report (the cleartext guidance in the same section remains valid and was re-verified: zero `usesCleartextTraffic`/`networkSecurityConfig` matches under `apps/android`).
- **Classification**: station lane = `BLOCKED_NO_CREDENTIALS` — the scripts exist and are current, but no `E2B_API_KEY` is in scope this wave; no fresh E2B run was performed and none is claimed (verbatim, per the PROD-033 m.e2b-fresh honesty law).

## Commands executed (exact list)

1. `git clone https://github.com/payswapdotorg/AISE.git` (→ `/home/z/AISE`; exit 0)
2. `git rev-parse HEAD` → `6a223f9fe74ea94138c4b62db8b228a615522e01`; `git log -1 --format='%H | %ci | %s'`; `git branch --show-current` (main); `git status --porcelain` (clean, re-verified at end)
3. Environment probe: `java -version`; `javac -version`; `which gradle adb sdkmanager emulator kotlinc`; `ls -la /dev/kvm`; `echo $ANDROID_HOME $ANDROID_SDK_ROOT`; `ls /usr/lib/jvm/*`; `df -h`; `nproc`; `free -h`
4. Structure: `tree apps/android -L 4 -I build` (fallback `ls -R`); `ls docs`; `ls docs/productization-evidence`; `ls PROD-033/runs PROD-032`
5. Reads (Read tool, with line numbers): `docs/TECH-LEAD-HANDOFF.md`; `docs/post-production-discoverability-and-device-validation-plan-2026-09-25.md`; `apps/android/README.md`; `docs/productization-evidence/PROD-032/physical-device-lane.md`; `PROD-032/README.md` (§1-2); `PROD-033/runs/m-2026-09-25T14-57-50Z.md`; `PROD-015/README.md` (head); `docs/android-cli-development-station.md`; `app/build.gradle.kts`; `app/src/main/AndroidManifest.xml`; `settings.gradle.kts`; root `build.gradle.kts`; `gradle/libs.versions.toml`; `gradle.properties`; `gradle/wrapper/gradle-wrapper.properties`; `core/build.gradle.kts`; app sources: `CaptureScreen.kt`, `CaptureViewModel.kt`, `FieldJourneyViewModel.kt`, `SettingsScreen.kt`, `AiseApp.kt`, `MainActivity.kt`, `AiseApplication.kt`, `AppContainer.kt`, `AppDestination.kt`, `CameraCaptureAdapter.kt`, `RotationVectorSnapshotter.kt`, `CaptureSessionController.kt`, `SessionRecovery.kt`, `JsonlSessionJournal.kt`, `HttpEvidenceSubmissionTransport.kt`, `MobileAuthClient.kt`, `FieldJourneyRuntime.kt`, `MainActivityInstrumentedTest.kt`; core sources: `FieldJourney.kt`, `SessionManifestExporter.kt`, `CapturedAssetRecord.kt`, `CapabilityNegotiation.kt` (partial), `MobileFieldAdapterProfile.kt` (partial); tests: `FieldJourneyStationSyncTest.kt` (head); station scripts: `e2b-station/README.md`, `gradle-trio.sh`, `field-journey.sh`, `emulator-probe.sh` (head), `bootstrap.sh` (greps), `station-driver.py` (greps), `android-cli-station.sh` (head)
6. Greps (Grep tool): `usesCleartextTraffic|networkSecurityConfig` over `apps/android` (0 matches); `rememberLauncherForActivityResult|RequestPermission|CAMERA`; `permission|Permission` in app main; `AISE_STATION_SYNC_BASE_URL|AISE_STATION_JOURNAL_PATH`; `adapterKind|browser` in `apps/web` + `packages/adapter-contract`
7. Git evidence: `git diff --stat 6728c3b..HEAD -- apps/android` (empty); `git diff --stat 6728c3b..HEAD -- backend packages/shared-contracts`; `git diff --stat 6728c3b..HEAD -- backend/api/src/capture backend/api/src/auth backend/api/src/session backend/api/src/ingestion` (empty); `git log --oneline 6728c3b..HEAD` (64 commits, docs/state/deploy only); `git rev-list --count 6728c3b..HEAD` (64)
8. Section B: `cd /home/z/AISE/apps/android && timeout 1200 ./gradlew :core:test --no-daemon` (nohup background, transcript `/home/z/post002-jvm-lane.log`; killed by the 1200 s timeout with no Gradle execution) + progress polls (`tail`, `du -sh ~/.gradle`, `pgrep`)
9. Artifact authoring: `mkdir -p /home/z/post002-artifacts` + wrote the three files (Write tool); worklog append at `/home/z/my-project/worklog.md`
10. Final repo integrity re-check: `git status --porcelain` (empty), `git rev-parse HEAD` (unchanged)

## Failure/unsupported cases (each with honest classification)

1. **Physical lane** — `BLOCKED_NO_PHYSICAL_HARDWARE`: no Android device in this sandbox; no `adb`. Recorded as a first-class result; nothing fabricated.
2. **Emulated lane** — `BLOCKED_NO_TOOLCHAIN`: no Android SDK/emulator/AVD present and no `/dev/kvm` (KVM-less sandbox cannot run an emulator; identical to the prior campaign's `BLOCKED_NO_KVM`). Zero minutes spent attempting a toolchain build (impossible target + ≤20-minute rule).
3. **JVM deterministic lane** — `BLOCKED_NO_TOOLCHAIN` (time-boxed): JRE-only (no `javac`), no Gradle, no cache; wrapper distribution download throttled (~13 MB of ~138 MB in 20 min); `timeout 1200` killed the wrapper before any Gradle execution. Zero test counts claimed.
4. **Station lane** — `BLOCKED_NO_CREDENTIALS`: scripts verified current (zero apps/android drift since `6728c3b`); no E2B key in scope; no fresh run performed or claimed.
5. **GAP-1 (defect, deterministic finding)** — stale user-facing copy: submission panel claims the sync transport is not wired (`CaptureScreen.kt:328-333`; also `:378-383`, `FieldJourneyRuntime.kt:40-46,119-123`, `AppContainer.kt:79-81`) while `AppContainer.kt:83-94` wires the real HTTP transport. Governed fix: copy/KDoc refresh work item. Not patched by me (read-only wave).
6. **GAP-2 (defect, deterministic finding)** — empty `onError` swallows still-capture failures at the UI surface (`CaptureScreen.kt:258`); session/journal stay honest but the operator gets no signal. Governed fix: route to `viewModel.message`.
7. **GAP-3 (planned scope gap, deterministic finding)** — no post-work capture entry point / no web-originated task identity / no deep links (`AppDestination.kt:13-17`; `FieldJourneyRuntime.kt:186-251`; `MobileFieldAdapterProfile.kt:53-56`). Governed route: POST-005 (Wave R1). Not a worker patch.
8. **Stale lane-doc instruction (doc drift)** — `PROD-032/physical-device-lane.md` §3 "Settings → server base URL" vs `SettingsScreen.kt:64` (read-only display). Governed fix: one-paragraph doc refresh; runbook §0.1 supersedes.

## Security/tenant considerations

- **No secrets touched, used, or emitted.** The clone is the public repo, read-only. No GitHub write credentials, no E2B key, no Vercel token were present or attempted; no authenticated cloud operation of any kind was performed.
- **No repo mutations**: working tree clean at every check; HEAD unchanged; the three Section C artifacts and the worklog were written OUTSIDE the clone (`/home/z/post002-artifacts/`, `/home/z/my-project/worklog.md`); the partial Gradle cache landed in `~/.gradle` (user scope, outside the repo).
- **Camera/photo evidence excludes secrets by construction** (and the protocol mandates it): session manifests carry only content hashes, byte sizes, media types, acquisition metadata, and device identity (`SessionManifestExporter.kt:58-105`); auth tokens are in-memory only (`MobileAuthClient.kt:24-25`) and never rendered; the station backend secret is per-run random and never transmitted (PROD-032 discipline); EVIDENCE-CLASSIFICATION-PROTOCOL.md §5 makes the no-secrets review a filing gate for the operator.
- **Tenant considerations**: the app is app-private-storage-only with `allowBackup=false` (`AndroidManifest.xml:26`) and a device identity scoped to app-data lifetime (README limitation, `:395-398`) — no cross-tenant leakage surface in the audited code; sync targets the demo-auth single-tenant deployment.

## Limitations

- No physical device, no emulator, no station: **every runtime Android behavior remains unverified this wave.** My evidence is a deterministic code-level audit plus committed prior evidence (cited, never upgraded). Nothing in this report is physical evidence.
- The JVM deterministic lane did not execute: even the deterministic runtime re-verification (`:core:test` 387 tests) is BLOCKED in this sandbox; the only executable deterministic lane was the code audit itself.
- The audit covers `apps/android` at the live HEAD; `apps/android` is byte-identical to `6728c3b`, so PROD-032 station records remain structurally applicable — but they are EMULATED evidence of an earlier run, not current-runtime proof.
- The live HEAD (`6a223f9`) differs from the dispatch-time SHA (`4065037`); both are recorded; the live value wins per the work order. The deployed URL (`https://aise-tan.vercel.app`) is pinned by POST-001 to `7968026`/`dpl_B7dnBrzQ2P3FzbVgdv8AhF814rU1` per the HEAD commit message — cross-SHA deployment hygiene is POST-001's lane, not mine.
- Time discipline honored: Section B hard-boxed to 20 minutes (aborted cleanly inside the box); total session well under the ~2h22m TTL, so no ~1h45m progress note was required.

## Out-of-scope (explicit non-goals)

- Any modification of AISE source code, docs, or state (read-only wave; defects reported with proposed governed fixes only).
- Any merge, push, branch, or release action; any deployment or alias operation (POST-001/POST-003 lanes).
- Wave R1 UX work (navigation vocabulary, Build Solution exposure, staged execution UX — POST-004), web→mobile handoff / BOQ revision selection / missing-evidence bridge / post-work return path (POST-005), acceptance/accessibility tests (POST-006).
- Emulator fabrication, physical-evidence fabrication, or any class upgrade (never).
- Re-running the E2B station or the deployed-browser checks without credentials.
- The sandbox's Next.js preview environment is not part of this Work Item and was not used as evidence.

## Successor handoff

1. **Operator with real hardware**: execute `/home/z/post002-artifacts/PHYSICAL-DEVICE-RUNBOOK.md` (reproduced in full above) top to bottom — choose sync lane A (default, `https://aise-tan.vercel.app`, no rebuild) or B (loopback + debug cleartext overlay + `adb reverse`), and fill one `EVIDENCE-RECORD-TEMPLATE.md` per run under the `EVIDENCE-CLASSIFICATION-PROTOCOL.md` rules (never upgrade classes; BLOCKED rows recorded; no secrets in artifacts).
2. **Tech Lead**: harvest the operator's filled records, independently reproduce what is reproducible (gradle trio at the exact SHA; server-side session GET), and file under `docs/productization-evidence/PROD-032/physical-device/` or a dated evidence directory; only then may physical-capture claims be made (plan §6 Wave 2 requires the physical lane for physical-capture claims).
3. **Lead files the governed fixes** for the reported defects: GAP-1 stale copy (`CaptureScreen.kt:328-333` et al.), GAP-2 empty onError (`CaptureScreen.kt:258`), GAP-3 post-work entry point (→ POST-005 scope), and the one stale lane-doc paragraph (`PROD-032/physical-device-lane.md` §3).
4. **Station re-run option**: when an `E2B_API_KEY` is in scope, `python3 station-driver.py up --repo-sha <live-HEAD> && … script gradle-trio && … script field-journey` remains valid as-is (scripts current; capture/auth backend routes unchanged since `6728c3b`).

=== END REPORT ===