# PROD-033 journey run — W

- recorded at: 2026-09-25T05:45:14Z
- repo SHA at run time: `a1bf2e6f065c37ada0bb972155d9437098f9c19f` (git rev-parse HEAD)
- base URL: http://localhost:4185 (local-serve)
- chromium: UNAVAILABLE — the browser legs fell back to their deterministic proofs (the Class column says so per leg; a live run is never fabricated)

Class discipline: the class is EXACTLY one of {deterministic, synthetic, emulated, physical} and names the EVIDENCE SOURCE; the parenthesized qualifier names the execution mode. Classes are never upgraded.

## W1 — the golden product journey (Gate F, journey 1)

```text
HOME → DEMO PROJECT → BOQ → EVIDENCE → CASE → INTERVENTION → OUTCOME
```

| # | Step | Verdict | Class |
|---|---|---|---|
| 1 | w1.serve — the local production-like serve (build → start → dark-before proof) | PASS | deterministic (fallback: no Chromium — the local serve still booted (build + start + healthz); the full runtime proof cited: tools/smoke.ts) |
| 2 | w1.availability — the base-agnostic availability probe (/, /healthz, /readyz) | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 3 | w1.session — the session lifecycle (mint → whoami → logout → fail-closed 401) | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 4 | w1.home — HOME — the gate, the task-first landing, the live provider note | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 5 | w1.demo-project — DEMO PROJECT — the live projects list → the project overview | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 6 | w1.boq — BOQ — the BOQ Lens + the SOURCE BOQ import panel + incumbent context | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 7 | w1.evidence — EVIDENCE — the capture surface + the guided mission + the upload entry | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 8 | w1.upload — the REAL capture upload round-trip (STORED → DUPLICATE) | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 9 | w1.case — CASE — the engineering case + the Evidence Envelope | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 10 | w1.intervention — INTERVENTION — the Intervention Studio + proposed states | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 11 | w1.outcome — OUTCOME — the Outcomes surface | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 12 | w1.responsive — desktop AND mobile viewport smoke (the imported deployed checks) | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 13 | w1.accessibility — accessibility at both viewports (the imported axe check) | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 14 | w1.console — the W1 console guard (zero page errors / blocking errors / blocking failures) | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |

### w1.serve — the local production-like serve (build → start → dark-before proof)

- verdict: PASS
- class: deterministic (fallback: no Chromium — the local serve still booted (build + start + healthz); the full runtime proof cited: tools/smoke.ts)
  - build: artifacts already present (apps/web/dist/index.html + api/[...path].mjs) — the repo's own `bun run build` output
  - pre-flight causality: ports 8795/4185 proven DARK before the serve started (TCP connect refused on both loopback addresses)
  - the serve answers /healthz on both origins (api http://127.0.0.1:8795, web http://localhost:4185)
  - the serve booted (bun run build + bun run start with a scratch data dir, the demo-open auth shape); the full runtime contract proof is tools/smoke.ts (SMOKE: PASS — cited)

### w1.availability — the base-agnostic availability probe (/, /healthz, /readyz)

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: tools/smoke.ts — GET /healthz + /readyz contract assertions (SMOKE: PASS)

### w1.session — the session lifecycle (mint → whoami → logout → fail-closed 401)

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: auth/middleware.test.ts › THE AUTHORIZATION MATRIX — path-scoped namespaces (GET + POST)
  - cited: auth/middleware.test.ts › the demo session mint + the fail-closed 401 family
  - bun test backend/api/src/auth/middleware.test.ts
  - 57 pass
  - 0 fail
  - Ran 57 tests across 1 file. [169.00ms]

### w1.home — HOME — the gate, the task-first landing, the live provider note

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: discoverability.test.tsx › the LANDING composes the task-first flow AND the contextual integrations panel
  - cited: provider-status.test.tsx › the /readyz extraction seam (cited)
  - bun test apps/web/src/app/task-first.test.tsx apps/web/src/app/discoverability.test.tsx
  - 23 pass
  - 0 fail
  - Ran 23 tests across 2 files. [210.00ms]

### w1.demo-project — DEMO PROJECT — the live projects list → the project overview

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: api.test.ts › loadProjectsLive — the live projects contract
  - bun test apps/web/src/app/api.test.ts
  - 52 pass
  - 0 fail
  - Ran 52 tests across 1 file. [67.00ms]

### w1.boq — BOQ — the BOQ Lens + the SOURCE BOQ import panel + incumbent context

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: discoverability.test.tsx › the BOQ import is discoverable from the journey and lives on the lens surface
  - cited: boq-import.test.tsx › the panel labels the import as a SOURCE BOQ
  - bun test apps/web/src/app/boq-import.test.tsx apps/web/src/app/discoverability.test.tsx
  - 22 pass
  - 0 fail
  - Ran 22 tests across 2 files. [180.00ms]

### w1.evidence — EVIDENCE — the capture surface + the guided mission + the upload entry

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: capture-mission.test.tsx › the capture SURFACE mounts the guided mission, the upload entry and the honest limits card
  - bun test apps/web/src/app/capture-mission.test.tsx
  - 18 pass
  - 0 fail
  - Ran 18 tests across 1 file. [139.00ms]

### w1.upload — the REAL capture upload round-trip (STORED → DUPLICATE)

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: capture/router.test.ts › upload → sync → read persists assets and the session verbatim
  - cited: capture/router.test.ts › re-uploading identical asset content succeeds without duplication
  - bun test backend/api/src/capture/router.test.ts
  - 17 pass
  - 0 fail
  - Ran 17 tests across 1 file. [175.00ms]

### w1.case — CASE — the engineering case + the Evidence Envelope

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: evidence-envelope.test.tsx › the envelope's honest absences (11 tests)
  - cited: discoverability.test.tsx › the evidence envelope is visible at BOTH consequential decisions
  - bun test apps/web/src/app/evidence-envelope.test.tsx apps/web/src/app/discoverability.test.tsx
  - 21 pass
  - 0 fail
  - Ran 21 tests across 2 files. [219.00ms]

### w1.intervention — INTERVENTION — the Intervention Studio + proposed states

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: discoverability.test.tsx › the 16-render corpus (the SiteTwin/Outcomes bodies verified in the same pass)
  - bun test apps/web/src/app/discoverability.test.tsx
  - 10 pass
  - 0 fail
  - Ran 10 tests across 1 file. [175.00ms]

### w1.outcome — OUTCOME — the Outcomes surface

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: outcome-forms.test.ts › the outcome surface's forms contract
  - bun test apps/web/src/app/discoverability.test.tsx apps/web/src/app/outcome-forms.test.ts
  - 27 pass
  - 0 fail
  - Ran 27 tests across 2 files. [179.00ms]

### w1.responsive — desktop AND mobile viewport smoke (the imported deployed checks)

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: PROD-012-R/remediation.md — the mobile-overflow + contrast fixes verified against the local production build
  - docs/productization-evidence/PROD-012-R/remediation.md — committed

### w1.accessibility — accessibility at both viewports (the imported axe check)

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: PROD-012/run-2026-09-20.txt — the deployed accessibility scan (axe, both viewports)
  - docs/productization-evidence/PROD-012-R/remediation.md — committed
  - docs/productization-evidence/PROD-012/run-2026-09-20.txt — committed

### w1.console — the W1 console guard (zero page errors / blocking errors / blocking failures)

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: PROD-031/browser-journey.md — ZERO pageerror events across the whole session (committed)
  - docs/productization-evidence/PROD-031/browser-journey.md — committed
  - docs/productization-evidence/PROD-030/browser-proof.md — committed

## W2 — the interactive solution journey (Gate F, journey 2)

```text
CURRENT BUILDING → PROBLEM → INTERACTIVE SOLUTION → VALIDATE → SOLUTION BOQ → BOQ LINE → SOLUTION STEP
```

| # | Step | Verdict | Class |
|---|---|---|---|
| 1 | w2.current-building — CURRENT BUILDING → PROBLEM — the solution route opens the demo case | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 2 | w2.solution-mount — INTERACTIVE SOLUTION — the workspace mounts (browser engine binding) | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 3 | w2.direct-manipulation — direct manipulation creates a typed operation | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 4 | w2.agent-turn — the agent command creates a semantically equivalent typed operation | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 5 | w2.validate — VALIDATE — deterministic validation tied to the solution version | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 6 | w2.solution-boq — SOLUTION BOQ — the generated BOQ renders (7 lines) | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 7 | w2.boq-line — BOQ LINE — the generated line selects | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 8 | w2.solution-step — SOLUTION STEP — the line→step deep link round-trip | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 9 | w2.reality-seal — the observed scene stays byte-identical (read-only reality) | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |
| 10 | w2.console — the whole-run console guard (zero page errors across both journeys) | PASS | deterministic (fallback: no Chromium — the cited committed suites ran) |

### w2.current-building — CURRENT BUILDING → PROBLEM — the solution route opens the demo case

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: solution-composition-model.test.tsx › the twelve-step composed journey through the REAL engine (the PROD-026 record's parity)
  - cited: solution-journey-record.test.ts › the committed record equals the LIVE Node run of the ONE runner
  - cited: service-prod031.test.ts › the HTTP binding's baseline/step/validate legs (the seam the browser mount speaks)
  - bun test apps/web/src/app/solution-composition-model.test.tsx apps/web/src/app/solution-journey-record.test.ts apps/web/src/solution/service-prod031.test.ts
  - 50 pass
  - 0 fail
  - Ran 50 tests across 3 files. [377.00ms]

### w2.solution-mount — INTERACTIVE SOLUTION — the workspace mounts (browser engine binding)

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: solution-composition-model.test.tsx › the twelve-step composed journey through the REAL engine (the PROD-026 record's parity)
  - cited: solution-journey-record.test.ts › the committed record equals the LIVE Node run of the ONE runner
  - cited: service-prod031.test.ts › the HTTP binding's baseline/step/validate legs (the seam the browser mount speaks)
  - bun test apps/web/src/app/solution-composition-model.test.tsx apps/web/src/app/solution-journey-record.test.ts apps/web/src/solution/service-prod031.test.ts
  - 50 pass
  - 0 fail
  - Ran 50 tests across 3 files. [377.00ms]

### w2.direct-manipulation — direct manipulation creates a typed operation

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: solution-composition-model.test.tsx › the twelve-step composed journey through the REAL engine (the PROD-026 record's parity)
  - cited: solution-journey-record.test.ts › the committed record equals the LIVE Node run of the ONE runner
  - cited: service-prod031.test.ts › the HTTP binding's baseline/step/validate legs (the seam the browser mount speaks)
  - bun test apps/web/src/app/solution-composition-model.test.tsx apps/web/src/app/solution-journey-record.test.ts apps/web/src/solution/service-prod031.test.ts
  - 50 pass
  - 0 fail
  - Ran 50 tests across 3 files. [377.00ms]

### w2.agent-turn — the agent command creates a semantically equivalent typed operation

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: solution-composition-model.test.tsx › the twelve-step composed journey through the REAL engine (the PROD-026 record's parity)
  - cited: solution-journey-record.test.ts › the committed record equals the LIVE Node run of the ONE runner
  - cited: service-prod031.test.ts › the HTTP binding's baseline/step/validate legs (the seam the browser mount speaks)
  - bun test apps/web/src/app/solution-composition-model.test.tsx apps/web/src/app/solution-journey-record.test.ts apps/web/src/solution/service-prod031.test.ts
  - 50 pass
  - 0 fail
  - Ran 50 tests across 3 files. [377.00ms]

### w2.validate — VALIDATE — deterministic validation tied to the solution version

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: solution-composition-model.test.tsx › the twelve-step composed journey through the REAL engine (the PROD-026 record's parity)
  - cited: solution-journey-record.test.ts › the committed record equals the LIVE Node run of the ONE runner
  - cited: service-prod031.test.ts › the HTTP binding's baseline/step/validate legs (the seam the browser mount speaks)
  - bun test apps/web/src/app/solution-composition-model.test.tsx apps/web/src/app/solution-journey-record.test.ts apps/web/src/solution/service-prod031.test.ts
  - 50 pass
  - 0 fail
  - Ran 50 tests across 3 files. [377.00ms]

### w2.solution-boq — SOLUTION BOQ — the generated BOQ renders (7 lines)

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: solution-composition-model.test.tsx › the twelve-step composed journey through the REAL engine (the PROD-026 record's parity)
  - cited: solution-journey-record.test.ts › the committed record equals the LIVE Node run of the ONE runner
  - cited: service-prod031.test.ts › the HTTP binding's baseline/step/validate legs (the seam the browser mount speaks)
  - bun test apps/web/src/app/solution-composition-model.test.tsx apps/web/src/app/solution-journey-record.test.ts apps/web/src/solution/service-prod031.test.ts
  - 50 pass
  - 0 fail
  - Ran 50 tests across 3 files. [377.00ms]

### w2.boq-line — BOQ LINE — the generated line selects

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: solution-composition-model.test.tsx › the twelve-step composed journey through the REAL engine (the PROD-026 record's parity)
  - cited: solution-journey-record.test.ts › the committed record equals the LIVE Node run of the ONE runner
  - cited: service-prod031.test.ts › the HTTP binding's baseline/step/validate legs (the seam the browser mount speaks)
  - bun test apps/web/src/app/solution-composition-model.test.tsx apps/web/src/app/solution-journey-record.test.ts apps/web/src/solution/service-prod031.test.ts
  - 50 pass
  - 0 fail
  - Ran 50 tests across 3 files. [377.00ms]

### w2.solution-step — SOLUTION STEP — the line→step deep link round-trip

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: solution-composition-model.test.tsx › the twelve-step composed journey through the REAL engine (the PROD-026 record's parity)
  - cited: solution-journey-record.test.ts › the committed record equals the LIVE Node run of the ONE runner
  - cited: service-prod031.test.ts › the HTTP binding's baseline/step/validate legs (the seam the browser mount speaks)
  - bun test apps/web/src/app/solution-composition-model.test.tsx apps/web/src/app/solution-journey-record.test.ts apps/web/src/solution/service-prod031.test.ts
  - 50 pass
  - 0 fail
  - Ran 50 tests across 3 files. [377.00ms]

### w2.reality-seal — the observed scene stays byte-identical (read-only reality)

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: solution-composition-model.test.tsx › the twelve-step composed journey through the REAL engine (the PROD-026 record's parity)
  - cited: solution-journey-record.test.ts › the committed record equals the LIVE Node run of the ONE runner
  - cited: service-prod031.test.ts › the HTTP binding's baseline/step/validate legs (the seam the browser mount speaks)
  - bun test apps/web/src/app/solution-composition-model.test.tsx apps/web/src/app/solution-journey-record.test.ts apps/web/src/solution/service-prod031.test.ts
  - 50 pass
  - 0 fail
  - Ran 50 tests across 3 files. [377.00ms]

### w2.console — the whole-run console guard (zero page errors across both journeys)

- verdict: PASS
- class: deterministic (fallback: no Chromium — the cited committed suites ran)
  - cited: PROD-031/browser-journey.md — ZERO pageerror events across the whole session (committed evidence)
  - docs/productization-evidence/PROD-031/browser-journey.md — committed

## The local serve teardown (the measurement-causality closing proof)

| # | Step | Verdict | Class |
|---|---|---|---|
| 1 | w.teardown — the serve teardown identity proof (both ports dark after) | PASS | deterministic (the smoke.ts causality doctrine — the ports must be dark again after the serve stopped) |

### w.teardown — the serve teardown identity proof (both ports dark after)

- verdict: PASS
- class: deterministic (the smoke.ts causality doctrine — the ports must be dark again after the serve stopped)
  - the serve orchestrator stopped (exit code 0); the scratch data dir removed
  - identity proof: ports 8795/4185 dark after teardown: true (anything still answering would mean the probes may have hit a foreign server — the run refuses that false positive)

## Run verdict

- steps: 25 PASS / 0 FAIL / 0 BLOCKED_NO_KVM (recorded)

JOURNEY W: PASS
