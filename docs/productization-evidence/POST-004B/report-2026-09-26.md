=== POST-004B COMPLETION REPORT ===
- Work Item ID: POST-004B (issue #18 follow-up)
- Base SHA: 0bb8c87898ee59ceb5c37784078d7f6bcce82f10
- Head SHA (WORKER_COMMIT): 4a8731ce4d8599e60b5c7f1676f38b109f6f2f1e
- Files changed: 8 (+960/-6)
- Items 1-3:
  - Item 1 (orphaned demo-solution world): DONE — a clearly-labeled "Build solution — the interactive walkthrough" card (SolutionWorldCard) now renders on the Projects surface in demo AND live mode, linking #/projects/proj-demo-001/solution directly with honest labeling (its own project, outside the organization's registry); shipped in apps/web/src/app/surfaces/Projects.tsx + 5 targeted assertions in apps/web/src/app/post004b-defect-fixes.test.tsx. Note honestly recorded: the nav "Build solution" entry and the Dashboard step-5 link already reached proj-demo-001 at this base (POST-004's merge cc02dba) — the Lead's "no in-app link reaches it" clause was POST-003-era; the residual gap closed here is the Projects surface in live mode, where the registry lists only the two seeded projects.
  - Item 2 (identity race): DONE — App.tsx ONLY: the exported pure predicate actingPrincipalUnresolved() gates the data surfaces on principal resolution; a signed-in session with no remembered principal (the cold already-authenticated new-tab load) renders the honest UnresolvedPrincipalPanel instead of letting requester-guarded identity reads fire with the demo-vocabulary default (the observed 422 mechanism); whoami stays display-only (AISE-036); the W journey's enter-demo navigation shape is provably undisturbed (25/0/0).
  - Item 3 (M-record header honesty): DONE — tools/journey/m.ts:218 now carries the strictly-additive union value "none-by-design"; tools/journey/classes.ts (field type + render branch) renders the by-design header while boolean W/X renderings stay byte-identical (pinned by tools/journey/classes.test.ts). Authorization deviation flagged: the reword option mechanically requires the shared renderer (classes.ts:96/:168 — two lines beyond "that line only"); the probe option was rejected as dishonest (it would fabricate a live-legs claim for a legless journey).
- Exit gates: verify="5984 pass / 0 fail — 79324 expect() calls, 390 files, VERIFY: PASS" (base 5969/0 + 15 new tests; no test weakened, skipped, or deleted), item-1 test="apps/web/src/app/post004b-defect-fixes.test.tsx — 11/11 PASS (incl. the 5 solution-world-affordance assertions: vocabulary+href, honesty labeling, no-dead-pointer, demo-mode wiring, live-mode wiring)", m-header="- chromium: not applicable — this journey has no browser legs by design (its evidence is the committed transcripts; the station's Chromium capability is never claimed as this journey's evidence)" — outcome classes identical to every committed M record (19 PASS / 0 FAIL / 1 BLOCKED_NO_KVM; 19 emulated rows). Bonus at the fix head: W journey 25 PASS / 0 FAIL / 0 BLOCKED_NO_KVM with its boolean header byte-identical.
- Delivery staged at: /home/z/my-project/post004b-delivery/ (files: 8 — byte-identical to the HEAD blobs, verified; bundle: 19792 bytes — aise-post004b.bundle, range base..HEAD, `git bundle verify` clean; MANIFEST lines: 14 — sha256 + path per staged file plus bundle and DELIVERY.txt)
- Commands executed (ordered): `git clone https://github.com/payswapdotorg/AISE.git` (refused — checkout already persisted at the pinned base; used it, tree clean); `git checkout -b post004b/defect-fixes` + `git rev-parse HEAD` (=0bb8c878…); `bunx playwright install chromium`; `bun install --frozen-lockfile`; `bun node_modules/playwright/cli.js install chromium`; `bun run verify` (baseline FAIL: typecheck — bun-types' `/// <reference types="node" />` escaped the repo to the parent sandbox scaffold's @types/node@25.0.9); `bun -e 'fs.unlinkSync/fs.symlinkSync'` environment repair: node_modules/@types/node -> ../.bun/@types+node@26.5.1/node_modules/@types/node (node_modules only, tree stays clean — the POST-004 worker's known repair); `bun run verify` (baseline green: 5969/0); file edits (m.ts, classes.ts, App.tsx, Projects.tsx, 2 new test files); `bun tools/verify.ts typecheck`; `bun test apps/web/src/app/post004b-defect-fixes.test.tsx tools/journey/classes.test.ts` (15/15); `bun run verify` (5984/0); `git add … && git commit` (b6f9985); `bun tools/journey/run.ts m` (M: PASS 19/0/1); `bun tools/journey/run.ts w` (W: PASS 25/0/0); `git add … && git commit` (4a8731c); `bun run verify` (final at HEAD: 5984/0); delivery staging (`cp`, `git bundle create`, `sha256sum`, `git bundle verify`, `git show HEAD:path | sha256sum` comparisons); `git diff --name-only base..HEAD` (protected-surface check: spec/**, bun.lock, root package.json all untouched); `git status --short` (pristine)
- Limitations: (1) item-3 scope deviation — classes.ts type+render widened beyond the m.ts:218 line (strictly additive; W/X byte-identical, test-pinned); Lead should confirm or re-scope. (2) The deeper structural fix for item 1 (re-homing proj-demo-001 under the demo tenant's seeded registry in backend/api/src/auth/demo.ts) was NOT taken — the work order prescribed the affordance, not re-seeding; backend untouched. (3) The item-2 gate holds ALL data surfaces (not only the identity projects-list fetch) because App.tsx cannot distinguish per-surface guarded reads without environment.ts changes (outside the authorization); the hold state resolves only through sign-out→sign-in/Enter-demo. (4) The transient 422 was not directly reproduced (POST-003's out-of-repo driver is gone); the fix targets the defect's mechanism and is verified by the predicate truth table, panel tests, and the unchanged W navigation shape. (5) M's m.e2b-fresh row still records no fresh E2B run (no E2B_API_KEY) and the emulator lane remains BLOCKED_NO_KVM — the expected honest states, unchanged. (6) The node_modules/@types/node symlink repair is per-pod: a recycled pod must re-apply it after `bun install --frozen-lockfile` (recorded in DELIVERY.txt). (7) Deployed-surface behavior (Projects affordance + hold panel on the live deployment) is Lead-owned evidence: local production-like serve proves it; the exact-SHA deploy+replay has not happened at this head.
- Successor handoff: POST-006 must re-verify — `bun run verify` at 4a8731c expecting the NEW floor 5984/0 (5969/0 + 15 added; nothing deleted); the M record's by-design chromium header with zero outcome-class drift (19/0/1, 19 emulated rows) and the W replay 25/0/0 (both records committed under docs/productization-evidence/PROD-033/runs/ at 02-04-10Z/02-04-35Z); the Projects-surface solution-world affordance in the DEPLOYED shape after the Lead's next exact-SHA deploy+replay; the principal-resolution hold panel on a genuine new-tab cold already-authenticated load (the one state a same-tab replay never enters); and the item-3 classes.ts union-value authorization question. Environment: re-apply the @types/node symlink repair after any fresh `bun install` before running verify.
=== END REPORT ===